# customer-intake-sop — update 2026-09-24

Source: Releases 3a + 3b live on production 2026-09-24 (`f000ac28`). Evidence from `C:/Project/NCT/wt-release-r1` (production code), not the registry code root `nct-layout`. Changeset: `.zyt/pending-2026-09-24.json` (205 ops; evidence-verify kept 205; sop-patch dry run: 201 apply, 4 needs-human, 0 refused, 0 rule conflicts, validation ok).

Rules: the page has no `rules[]`, so there are no rule conflicts.

## Step 16

- **op-16-001** set `/guide/15/navigate`
  - old: Left rail, **You** block → **Shipment** → **Add**; the form is at /lading/create
  - new: From the received order's own page press **Raise bill of lading** (it opens the form pointed at that order), or left rail, **You** block → **Shipment** → **Add**; the form is at /lading/create
  - evidence: apps/web/src/components/order-ledger/order-record-page.tsx:152 "Raise bill of lading"; apps/web/src/routes/_next/lading/create.tsx:47 "the order record page's "Raise bill of lading" opens this form"
- **op-16-002** set `/guide/15/before/0`
  - old: The order exists and is received (steps 13–15). Note its order number: there is no route from the job to its B/L.
  - new: The order exists and is received (steps 13–15). Only a received, not-archived order shows **Raise bill of lading** and appears in the form's order list.
  - evidence: apps/web/src/components/order-ledger/order-record-page.tsx:149 "raw.acceptStatus === "received" && !raw.archived"; apps/web/src/routes/_next/lading/create.tsx:213 "acceptStatus: "received", archived: false"
- **op-16-006** set `/guide/15/golden/2/detail`
  - old: Toast: **Extracted fields applied to form**. Check every field against the scan.
  - new: Toast: **Extracted fields applied to form**. If OCR's own checks flag an error, **Apply to Form** stays disabled until you tick **Apply anyway: I have checked the flagged values against the scan**; warnings alone do not block it. Check every field against the scan.
  - evidence: apps/web/src/routes/_next/lading/create.tsx:1711 "Apply anyway: I have checked the flagged values against the scan"; apps/web/src/routes/_next/lading/create.tsx:1676 "disabled={!canApplyOcr(ocrIssues, ocrAcknowledged, ocrUpload.isPending)}"; apps/web/src/routes/_next/lading/-ocr-issues.ts:55 "blocksApply: errors.length > 0"
- **op-16-014** set `/guide/15/golden/8/detail`
  - old: A dropdown of every order in scope — there is no row limit on it. There is also a free-text **Order No** field, matched as an exact string.
  - new: Only received, not-archived orders are listed. Picking one fills the blank fields from the order, each marked **from order**, including **Order No** (free text, matched as an exact string).
  - evidence: apps/web/src/routes/_next/lading/create.tsx:1402 "Only received orders are listed."; apps/web/src/routes/_next/lading/create.tsx:213 "acceptStatus: "received", archived: false"; apps/web/src/routes/_next/lading/create.tsx:616 "from order"
- **op-16-017** set `/guide/15/fields/3/note`
  - old: Dropdown of every order in scope
  - new: Received, not-archived orders only; a pick fills blank fields from the order
  - evidence: apps/web/src/routes/_next/lading/create.tsx:1402 "Only received orders are listed."
- **op-16-018** set `/guide/15/pitfalls/0`
  - old: There is no route from the job to its B/L: the bill has to re-find its order in one flat dropdown of every order in scope, or match a job number typed as free text.
  - new: **Raise bill of lading** appears only on a received, not-archived order. Opened for any other order, the form says a bill of lading cannot be raised from it and leaves the bill unlinked.
  - evidence: apps/web/src/routes/_next/lading/create.tsx:1399 "so a bill of lading cannot be raised from it"; apps/web/src/components/order-ledger/order-record-page.tsx:149 "raw.acceptStatus === "received" && !raw.archived"
- **op-16-008** insert `/guide/15/golden/3/detail`
  - new: **Status** offers only Draft, Confirmed or Cancelled. On hold, Amended and Released are set by the actions on the bill's page (step 17).
  - evidence: apps/web/src/routes/_next/lading/create.tsx:168 "const STATUS_OPTIONS = ["draft", "confirmed", "cancelled"];"; packages/api/src/modules/lading/status.ts:13 "export const SETTABLE_LADING_STATUSES = ["draft", "confirmed", "cancelled"] as c"; packages/api/src/routers/lading.ts:1099 "assertSettableStatus(input.status);"
- Re-anchored citations (auto): 11

## Step 17

- **op-17-001** set `/steps/16/what`
  - old: Check In (endorsement), Amend, Check Out (release) and Hold (detention), each with a cancel twin and each behind its own gate. Per row, or from the detail page — the list’s tick-boxes drive review only. Scanned paperwork files as attachments on the bill itself.
  - new: Check In (endorsement), Amend, Check Out (release) and Hold (detention), each with a cancel twin and each behind its own gate. Per row, or from the detail page — the list’s tick-boxes drive review only. Send the bill for review from <strong>Review</strong> on its page; while under review it is frozen. Scanned paperwork files as attachments on the bill itself.
  - evidence: apps/web/src/routes/_next/lading/$id/index.tsx:453 "<ReviewMenu selectedIds={[id]} onReview={reviewLading} />"; apps/web/src/components/review-menu.tsx:50 "label: "Submit for review""
- **op-17-007** set `/guide/16/golden/3/detail`
  - old: Toast: **Checked out**; reverse with **Cancel Check Out**.
  - new: Toast: **Checked out**; reverse with **Cancel Check Out**. A bill on hold is refused (**On hold — release the hold before checking out**): press **Cancel Hold** first.
  - evidence: packages/api/src/routers/lading.ts:582 ""On hold — release the hold before checking out""
- **op-17-009** set `/guide/16/golden/4/detail`
  - old: Toast: **Put on hold**; release it with **Cancel Hold** (**Hold released**).
  - new: The **Put on hold** dialog asks for a **Reason (optional)**, shown on the bill beside the hold. Toast: **Put on hold**; release it with **Cancel Hold** (**Hold released**).
  - evidence: apps/web/src/components/lading/hold-reason.ts:13 "label: "Reason (optional)""; apps/web/src/components/lading/lading-record.tsx:358 "On hold since"; apps/web/src/routes/_next/lading/$id/index.tsx:237 "toast.success("Hold released");"
- **op-17-011** set `/guide/16/golden/5/detail`
  - old: Scans upload against the lading record itself.
  - new: Scans upload against the lading record itself. While the bill is under review you can still upload, but not remove one.
  - evidence: apps/web/src/routes/_next/lading/$id/index.tsx:535 "disabled={data.underReview}"; packages/api/src/routers/lading.ts:2327 "await assertNotUnderReview(tx, organizationId, "lading", input.ladingId);"
- **op-17-016** set `/guide/16/pitfalls/0`
  - old: The review half is missing: no shipment screen can submit a lading for review, so **Approvals → Shipment review** can only ever decide submissions that cannot be created.
  - new: A bill under review is frozen: **Save Changes** is disabled and attachments cannot be removed. Choose **Request withdrawal** from **Review** before changing it.
  - evidence: apps/web/src/routes/_next/lading/$id/edit.tsx:1780 ""Save Changes""; packages/api/src/routers/lading.ts:1693 "await assertNotUnderReview(tx, organizationId, "lading", ladingId);"
- **op-17-018** set `/guide/16/writes/0`
  - old: The bill's state and its cancel twin, each stamped; attachments on the record
  - new: The bill's state and its cancel twin, each stamped (a hold with its optional reason); attachments on the record; a review submission when sent for review
  - evidence: packages/api/src/routers/lading.ts:611 "patch: (_row, now, reason) => ({ heldAt: now, heldReason: reason ?? null }),"; apps/web/src/routes/_next/lading/$id/index.tsx:71 "const reviewLading = engineReview("lading");"
- **op-17-002** remove `/steps/16/watch`
  - removed: {"tag":"Watch","html":"The review half is missing: no shipped screen can submit a lading for review, so the Shipment review queue can only ever decide submissions that cannot be created."}
  - evidence: apps/web/src/routes/_next/lading/$id/index.tsx:453 "<ReviewMenu selectedIds={[id]} onReview={reviewLading} />"; apps/web/src/routes/_next/lading/$id/index.tsx:71 "const reviewLading = engineReview("lading");"
- **op-17-014** insert `/guide/16/golden/8`
  - new: {"action":"When the bill is ready for sign-off, open **Review** and choose **Submit for review**","detail":"While it is under review the page reads **Under review. Request withdrawal from the Review menu to make changes.** and **Save Changes** on the edit form is disabled. To change it, choose **Request withdrawal** first.","src":"apps/web/src/routes/_next/lading/$id/index.tsx:453"}
  - evidence: apps/web/src/routes/_next/lading/$id/index.tsx:453 "<ReviewMenu selectedIds={[id]} onReview={reviewLading} />"; apps/web/src/components/review-menu.tsx:55 "label: "Request withdrawal""; apps/web/src/components/order-ledger/under-review-notice.tsx:29 "Under review. Request withdrawal from the Review menu to make changes."; apps/web/src/routes/_next/lading/$id/edit.tsx:1777 "disabled={updateMutation.isPending || underReview}"
- **op-17-015** remove `/guide/16/fields/1`
  - removed: {"label":"Audit Status","required":false,"note":"Editable on the create and edit forms and persisted, but it is a cache column: no screen can queue a lading for review"}
  - evidence: apps/web/src/routes/_next/lading/create.tsx absent "Audit Status"; apps/web/src/routes/_next/lading/$id/edit.tsx absent "Audit Status"
- **op-17-017** insert `/guide/16/pitfalls/2`
  - new: **Check Out** on a held bill is refused. Press **Cancel Hold** first.
  - evidence: packages/api/src/routers/lading.ts:582 ""On hold — release the hold before checking out""
- Re-anchored citations (auto): 8

## Step 18

- **op-18-002** set `/guide/17/golden/0/detail`
  - old: The pipeline classifies the file, extracts its fields and validates them, then either auto-approves it or parks it as **needs review**.
  - new: The pipeline classifies the file, extracts its fields and validates them, then either auto-approves it or parks it as **Needs review**.
  - evidence: apps/web/src/routes/_next/documents.index.tsx:377 "<SelectItem value="needs_review">Needs review</SelectItem>"
- **op-18-007** set `/guide/17/golden/6`
  - old: {"action":"Misclassified? Press **Reclassify** to send it back through classification","detail":"Toast: **Sent back through classification**.","src":"apps/web/src/routes/_next/documents.$documentId.tsx:249"}
  - new: {"action":"Wrong type? Pick the right one in the type select beside the filename and press **Save & approve**","detail":"Re-typing a document to **MBL/HBL** starts its tracking job on approval. After approval the type is fixed. A document set aside as not freight offers **This is a freight document — reprocess** instead (toast **Sent back through classification**).","src":"apps/web/src/routes/_next/documents.$documentId.tsx:530"}
  - evidence: apps/web/src/routes/_next/documents.$documentId.tsx:527 "(step 18 D5-A). After approval the type is fixed (D10)."; apps/web/src/routes/_next/documents.$documentId.tsx:599 ""Save & approve""; apps/web/src/routes/_next/documents.$documentId.tsx:704 "This is a freight document — reprocess"; apps/web/src/routes/_next/documents.$documentId.tsx:302 "toast.success("Sent back through classification");"; apps/web/src/lib/documents/labels.ts:24 "mbl_hbl: "MBL/HBL","; apps/web/src/routes/_next/documents.$documentId.tsx absent "Reclassify"
- **op-18-008** set `/guide/17/golden/7/action`
  - old: Not freight at all? Press **Reject** and give a reason
  - new: Not freight at all? Press **Not freight** and give a reason
  - evidence: apps/web/src/routes/_next/documents.$documentId.tsx:577 "Not freight"
- **op-18-012** set `/guide/17/writes/0`
  - old: Document status (approved / needs review / rejected), the field corrections, and — on approval — a BL tracking job
  - new: Document status (approved / needs review / rejected), the field corrections, any order or bill of lading links, and — on approval — a BL tracking job
  - evidence: apps/web/src/routes/_next/documents.$documentId.tsx:1192 "client.document.link({ documentId, entityType: target.kind, entityId: target.id "
- **op-18-010** insert `/guide/17/golden/8`
  - new: {"action":"Under **Linked records**, choose **Order** or **Bill of lading**, search, pick it and press **Link**","detail":"Toast: **Linked to order …** or **Linked to bill of lading …**. **Unlink** removes a link (**Link removed**).","src":"apps/web/src/routes/_next/documents.$documentId.tsx:936"}
  - evidence: apps/web/src/routes/_next/documents.$documentId.tsx:936 "Linked records"; apps/web/src/routes/_next/documents.$documentId.tsx:1016 "collective_order: "Order","; apps/web/src/routes/_next/documents.$documentId.tsx:1017 "lading: "Bill of lading","; apps/web/src/routes/_next/documents.$documentId.tsx:1267 ""Link""; apps/web/src/routes/_next/documents.$documentId.tsx:1198 "`Linked to order ${target.label}`"; apps/web/src/routes/_next/documents.$documentId.tsx:1078 "toast.success("Link removed");"
- **op-18-011** remove `/guide/17/pitfalls/1`
  - removed: Paperwork cannot be filed against the order it belongs to — the only screen that links anything sends the type `quote`, so the per-order Documents card never renders.
  - evidence: packages/api/src/routers/document.ts:109 "z.enum(["quote", "collective_order", "lading"])"; apps/web/src/routes/_next/documents.$documentId.tsx:1192 "client.document.link({ documentId, entityType: target.kind, entityId: target.id "
- Re-anchored citations (auto): 6

## Step 19

- **op-19-003** set `/guide/18/golden/4/action`
  - old: Open a job to see its stages and containers
  - new: Click a **BL Number** to open its job
  - evidence: apps/web/src/routes/_next/customs-tracking.tsx:268 "to="/customs-tracking/$jobId""; apps/web/src/routes/_next/customs-tracking.tsx:272 "{job.blNumber}"
- **op-19-004** set `/guide/18/golden/4/detail`
  - old: The detail page is /customs-tracking/<jobId>.
  - new: The job page shows its stages and containers, its **Order** (found by B/L number), and **Fees** → **Open the order's fees** when exactly one order carries the B/L.
  - evidence: apps/web/src/components/customs-tracking/bl-job-record.tsx:267 "label: "Order","; apps/web/src/components/customs-tracking/bl-job-record.tsx:302 "label: "Fees","; apps/web/src/components/customs-tracking/bl-job-record.tsx:309 "Open the order's fees"
- **op-19-006** set `/steps/18/watch/html`
  - old: Tracking jobs are anchored to the <em>document</em> only — no order, no lading — and the board cannot reach that job's fees. The server does match jobs to orders by B/L number, but leaves the deadline out of that payload, and the card meant to show it never renders — so the countdown is still invisible from the order it belongs to.
  - new: Tracking jobs still store no order: a job finds its order, and an order its job, by B/L number. When two open jobs share one B/L number the order's card shows <strong>Match not certain</strong> and no clock, and where no portal login is saved the board says checks are off — nothing is being checked.
  - evidence: apps/web/src/components/customs-tracking/bl-job-record.tsx:251 "A job stores no order id"; apps/web/src/features/order-documents/order-documents-card.tsx:177 "Match not certain"; apps/web/src/features/customs-tracking/source-notice.ts:52 "Customs and port checks are off"
- **op-19-007** set `/guide/18/pitfalls/0`
  - old: Tracking jobs anchor to the document only — no order, no lading — so the board cannot reach that job's fees.
  - new: Where no MYCIEDS or SBCP login is saved, the board, the job page and the Overview card say checks are off: nothing is checked and no 72-hour clock can start. An admin adds the login in **Settings → Tracking portal credentials**.
  - evidence: apps/web/src/features/customs-tracking/source-notice.ts:46 "An admin adds {it} in Settings → Tracking portal credentials."; apps/web/src/features/customs-tracking/source-notice.ts:57 "no 72-hour clock can start"; apps/web/src/routes/_next/customs-tracking.tsx:198 "{notice.text}"; apps/web/src/components/shell-next/card-gateout.tsx:292 "sourceNoticeShort(checksOff)"
- **op-19-008** set `/guide/18/pitfalls/1`
  - old: The server does match tracking jobs to orders by B/L number but leaves the countdown deadline out of that payload, so the order's own documents card renders without its countdown chip — the clock stays invisible from the job it belongs to.
  - new: The order's reading pane and the B/L page show a **Documents & customs** card with the job's stage and 72h countdown; its B/L number opens the job. If two open jobs share the B/L number it shows **Match not certain** and no countdown.
  - evidence: apps/web/src/components/order-ledger/order-ledger-page.tsx:1211 "title: "Documents & customs","; apps/web/src/routes/_next/lading/$id/index.tsx:483 "<OrderDocumentsCard rollup={orderDocuments} />"; apps/web/src/features/order-documents/order-documents-card.tsx:217 "{chip.label}"; apps/web/src/features/order-documents/order-documents-card.tsx:229 "to="/customs-tracking/$jobId""; apps/web/src/features/order-documents/order-documents-card.tsx:177 "Match not certain"; packages/api/src/routers/collective-order.ts:1992 "countdownDeadline: row.countdownDeadline,"
- **op-19-005** insert `/guide/18/golden/4/src`
  - new: apps/web/src/components/customs-tracking/bl-job-record.tsx:267
  - evidence: apps/web/src/components/customs-tracking/bl-job-record.tsx:267 "label: "Order","
- Re-anchored citations (auto): 2

## Step 20

- **op-20-004** set `/guide/19/golden/2/detail`
  - old: New row defaults: Quantity 1, Currency USD, Exchange rate 1, Unit price/Total price/Tax rate 0; **Settlement unit** auto-fills with the order's Client (receivable side only).
  - new: New row defaults: Quantity 1, Currency USD, Unit price/Total price/Tax rate 0; **Exchange rate** is left blank (**From rate table**) and the server fills it on Save; **Settlement unit** auto-fills with the order's Client (receivable side only).
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:359 "quantity: "1","; apps/web/src/routes/_next/order.$orderId.expenses.tsx:366 "currency: "USD","; apps/web/src/routes/_next/order.$orderId.expenses.tsx:367 "exchangeRate: "","; apps/web/src/routes/_next/order.$orderId.expenses.tsx:496 ""From rate table""; apps/web/src/routes/_next/order.$orderId.expenses.tsx:790 "the server seeds it from"
- **op-20-006** set `/guide/19/golden/3/detail`
  - old: Total price recomputes as Quantity x Unit price when either changes; Taxes and No tax unit price are derived. Enter moves down a cell, Shift+Enter moves up.
  - new: Total price recomputes as Quantity x Unit price when either changes; typing a different Total clears Unit price, so the fee saves as a lump sum. Taxes and No tax unit price are derived. Enter moves down a cell, Shift+Enter moves up.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:261 "if (key === "quantity" || key === "unitPrice") {"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:294 "const onTotalBlur = (index: number) => {"; apps/web/src/lib/fee-rows.ts:166 "cleared: the line is a lump sum, saved with `unit_price` NULL"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:311 "if (e.key !== "Enter") return;"
- **op-20-008** set `/guide/19/golden/4/detail`
  - old: Copies only rows with a name, as new rows; **Settlement unit** is blanked on the payable copies so you must type the vendor.
  - new: Copies only rows with a name, as new rows; **Settlement unit** is blanked on the payable copies so you must type the vendor, and **Exchange rate** is blanked so the server prices each copy on its own side.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:808 ".filter((r) => r.costName.trim() !== "")"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:824 "exchangeRate: "","; apps/web/src/routes/_next/order.$orderId.expenses.tsx:830 "settlementUnit: toPayable ? "" : r.settlementUnit,"
- **op-20-010** set `/guide/19/golden/5/detail`
  - old: Payable new rows have no Settlement unit default.
  - new: Payable new rows have no Settlement unit default. A payable with no Settlement unit shows a warning above the grid (**Payable row n has no Settlement unit, so no vendor is named. It saves as it is; …**); it still saves.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:1007 "warning={payableNoVendor}"; apps/web/src/lib/fee-rows.ts:231 "has no Settlement unit, so no vendor is named. It saves as it is;"
- **op-20-012** set `/guide/19/golden/7/action`
  - old: If the banner **The exchange rate for <period> has not been set for <currencies>.** appears, follow **Please go to set it**.
  - new: If the banner **No exchange rate on file for <currencies>.** appears after Save, follow **Please go to set it**.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:960 "No exchange rate on file for {lastUnrated.join(", ")}."; apps/web/src/routes/_next/order.$orderId.expenses.tsx:963 "Please go to set it"
- **op-20-013** set `/guide/19/golden/7/detail`
  - old: Links to /expenses/exchange-rates; checks each currency pair against the owning branch's currency.
  - new: Links to /expenses/exchange-rates. It is the server's answer from the last Save: new fees in those currencies found no rate for the shipment's dates and count at rate 1 until one is set.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:962 "<Link to="/expenses/exchange-rates" className="underline">"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:612 "resolved by the shipment's dates into the"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:708 "setLastUnrated([...(result.unratedCurrencies ?? [])]);"
- **op-20-016** set `/guide/19/golden/8/detail`
  - old: One Save submits BOTH grids; toast **Fees saved**. Rows with an empty Name of cost are silently dropped.
  - new: One Save submits BOTH grids; toast **Fees saved** (it adds a note when fees added elsewhere since you opened the page were kept). An untouched blank row is dropped; a nameless row with anything typed in it refuses the Save by row number.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:707 "toast.success(notice ? `Fees saved. ${notice}` : "Fees saved");"; apps/web/src/lib/fee-rows.ts:146 "fees added elsewhere since you opened this page were kept."; apps/web/src/lib/fee-rows.ts:128 "but no Name of cost. Name it, or remove the row with"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:748 "toast.error("
- **op-20-018** set `/guide/19/before/3`
  - old: An exchange rate exists for the current month for every non-branch currency used, or the FX banner will warn.
  - new: An exchange rate is on file for every foreign currency used, for the shipment's dates. Otherwise new fees in that currency save at rate 1 and a banner says so after Save.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:612 "resolved by the shipment's dates into the"; packages/api/src/routers/collective-order.ts:4702 "Step 20 D5-A: the currencies whose seed MISSED."; apps/web/src/routes/_next/order.$orderId.expenses.tsx:960 "No exchange rate on file for"
- **op-20-020** set `/guide/19/fields/0/note`
  - old: Free text; placeholder 'Must fill in, otherwise it won't be saved'. Blank rows are dropped on save.
  - new: Free text; placeholder 'Must fill in, otherwise it won't be saved'. An untouched blank row is dropped; a nameless row with content, or a saved fee whose name you cleared, refuses the Save.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:494 "Must fill in, otherwise it won't be saved"; apps/web/src/lib/fee-rows.ts:90 "if (row.id) problems.push({ index, kind: "saved-unnamed" });"; apps/web/src/lib/fee-rows.ts:91 "else if (hasFeeContent(row)) problems.push({ index, kind: "unnamed-with-content""
- **op-20-021** set `/guide/19/fields/1/note`
  - old: Free text counterparty name (no company picker, no company id saved). Auto-filled with Client on receivable rows. Hidden if the role is field-masked.
  - new: Free text counterparty name (no company picker). Auto-filled with Client on receivable rows; a receivable naming the Client is linked to the client's company record. Hidden if the role is field-masked.
  - evidence: packages/api/src/routers/collective-order.ts:4781 "companyPatch = { settlementCompanyId: order.clientCompanyId };"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:992 "defaultSettlementUnit={String(order?.clientName ?? "")}"
- **op-20-022** set `/guide/19/fields/5/note`
  - old: Default 1.
  - new: Blank on a new row (**From rate table**); the server fills it from the rate table on Save. Clearing it on a saved row means 1.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:367 "exchangeRate: "","; apps/web/src/routes/_next/order.$orderId.expenses.tsx:496 ""From rate table""; apps/web/src/routes/_next/order.$orderId.expenses.tsx:790 "On a saved row a blanked cell still means "clear to 1"."
- **op-20-023** set `/guide/19/fields/7/note`
  - old: Auto = Quantity x Unit price (rounded to 6 dp); can be typed when quantity is blank.
  - new: Auto = Quantity x Unit price (rounded to 6 dp). Typing a different total clears Unit price and saves the fee as a lump sum.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:270 "r.amount = String(Number((num(r.quantity) * num(r.unitPrice)).toFixed(6)));"; apps/web/src/lib/fee-rows.ts:166 "cleared: the line is a lump sum, saved with `unit_price` NULL"
- **op-20-024** set `/guide/19/writes/0`
  - old: cost_line rows (attribute receivable/payable) via collectiveOrder.saveChildren, reconciled by id: new rows inserted, edited rows updated, rows removed from the grid deleted.
  - new: cost_line rows (attribute receivable/payable) via collectiveOrder.saveChildren, reconciled by id: new rows inserted, edited rows updated, rows you removed deleted. Only fees loaded when the page opened can be deleted; fees added elsewhere since are kept, and a fee deleted elsewhere refuses the Save.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:763 "loadedCostIds: baselineIds.current,"; packages/api/src/routers/collective-order.ts:4329 "keptUnseen = existing.filter((r) => !loaded.has(r.id)).length;"; packages/api/src/routers/collective-order.ts:4326 "were deleted by someone else after you opened it. Reload to see the current fees"
- **op-20-025** set `/guide/19/writes/1`
  - old: Settlement unit stored as a plain string; settlementCurrency/settlementExchangeRate are never sent from this page.
  - new: Settlement unit stored as a string; a receivable naming the order's Client also gets the client's company record. settlementCurrency/settlementExchangeRate are never sent from this page.
  - evidence: packages/api/src/routers/collective-order.ts:4781 "companyPatch = { settlementCompanyId: order.clientCompanyId };"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:770 "`settlementCurrency`/`settlementExchangeRate` are deliberately ABSENT:"
- **op-20-027** set `/guide/19/pitfalls/1`
  - old: A row whose Name of cost is blank is silently not saved (no error).
  - new: An untouched blank row is dropped without a word; a nameless row with anything typed in it, or a saved fee whose name you cleared, refuses the Save and names the row.
  - evidence: apps/web/src/lib/fee-rows.ts:128 "but no Name of cost. Name it, or remove the row with"; apps/web/src/lib/fee-rows.ts:116 "is a saved fee. Give it a name, or remove it with"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:748 "toast.error("
- **op-20-028** set `/guide/19/pitfalls/2`
  - old: Rows already billed, invoiced or written off are read-only and cannot be removed here.
  - new: Rows already billed, invoiced, written off or locked are read-only and cannot be removed here; unlock a locked fee on Money > Cost lines.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:190 "This fee is locked and cannot be edited or removed here; unlock it on Money > Co"; apps/web/src/routes/_next/order.$orderId.expenses.tsx:185 "const isFrozen = (r: FeeRow) => isSettled(r) || isLocked(r);"
- **op-20-019** insert `/guide/19/before/4`
  - new: No fee you are changing is under review. A fee under review is frozen; withdraw its submission first on **Money > Cost lines**.
  - evidence: packages/api/src/modules/expense/review-freeze.ts:43 "if (status === "under_review" || status === "withdrawal_under_review") {"; packages/api/src/modules/expense/review-freeze.ts:75 "are under review and cannot be ${verb} here. Withdraw the submission first:"; packages/api/src/routers/collective-order.ts:4543 "const freezes = await costLineReviewFreezes("
- **op-20-026** insert `/guide/19/writes/3`
  - new: A new fee's exchange rate is filled by the server from the rate table for the shipment's dates; with no rate on file it saves at 1 (refused if the org forbids unrated fees).
  - evidence: packages/api/src/routers/collective-order.ts:4688 "const resolved = await resolveRate(tx, organizationId, {"; packages/api/src/routers/collective-order.ts:4720 "await assertUnratedFeeAllowed(tx, organizationId, unratedCurrencies.length > 0);"
- **op-20-029** insert `/guide/19/pitfalls/7`
  - new: A fee under review is frozen: changing or removing it refuses the Save with **… are under review and cannot be changed here. Withdraw the submission first**. A fee approved under a flow that locks approved content cannot be changed either; add a correcting fee line.
  - evidence: packages/api/src/modules/expense/review-freeze.ts:75 "are under review and cannot be ${verb} here. Withdraw the submission first:"; packages/api/src/modules/expense/review-freeze.ts:76 "Add a correcting fee line, or ask an administrator to allow edits after approval"; packages/api/src/routers/collective-order.ts:4558 "message: reviewFreezeSentence(kind, "changed", frozenEdited.map(frozenFeeName)),"
- **op-20-030** insert `/guide/19/pitfalls/8`
  - new: If someone deleted one of these fees after you opened the page, Save is refused; reload to see the current fees.
  - evidence: packages/api/src/routers/collective-order.ts:4326 "were deleted by someone else after you opened it. Reload to see the current fees"
- Re-anchored citations (auto): 10

## Step 21

- **op-21-001** set `/steps/20/what`
  - old: Submit from the fee ledger’s Review menu; decide here, on a toolbar of Approved / Rejected / Refresh Page. The fee ledger’s own toolbar — not this one — carries reconcile, lock, archive, tag, and an exchange-rate dialog that applies a rate you type by hand.
  - new: Submit from the fee ledger’s Review menu; someone else decides here, on a toolbar of Approved / Rejected / Refresh Page — nobody decides their own fee. The fee ledger’s own toolbar — not this one — carries reconcile, lock, archive, tag, and an exchange-rate dialog that applies a rate you type by hand.
  - evidence: packages/api/src/modules/audit/resources.ts:635 "legacy costLines.review let an accountant approve their own fee"; packages/api/src/modules/audit/resources.ts:638 "separationOfDuties: true,"; apps/web/src/components/review-queue.tsx:362 "Approved"
- **op-21-002** set `/guide/20/role`
  - old: Submitter: expense:read to see the ledger, plus audit.read and expense.costLine.update to submit. Reviewer: audit.read and expense.costLine.review, and must be a routed reviewer on the open stage (seeded Cost review flow routes to the accounting role, any-pass).
  - new: Submitter: expense:read to see the ledger, plus audit.read and expense.costLine.update to submit. Reviewer: audit.read and expense.costLine.review, must be a routed reviewer on the open stage (seeded Cost review flow routes to the accounting role, any-pass), and must not be the member who submitted the line.
  - evidence: packages/api/src/modules/audit/resources.ts:580 "reviewNode: EXPENSE.costLineReview,"; packages/api/src/modules/audit/decide.ts:59 "if (entry.separationOfDuties && submission.submittedBy === org.membership.id) {"; packages/api/src/modules/audit/decide.ts:61 "You submitted this record. Another reviewer must decide it."
- **op-21-006** set `/guide/20/golden/3/detail`
  - old: Selection-only fee toolbar verbs. Modify exchange rate skips billed, invoiced, written-off or locked lines and lists them back.
  - new: Selection-only fee toolbar verbs. Modify exchange rate skips lines that are billed, invoiced, written off or locked, under review, or approved and locked, and lists them back with the reason.
  - evidence: apps/web/src/components/expense/cost-line-batch-menu.tsx:309 "type SkipReason = "settled" | "under_review" | "approved";"; apps/web/src/components/expense/cost-line-batch-menu.tsx:313 "under_review: "under review","; apps/web/src/components/expense/cost-line-batch-menu.tsx:314 "approved: "approved and locked","
- **op-21-007** set `/guide/20/golden/4/detail`
  - old: Mounts the same ledger in review mode, titled Cost review, restricted to submissions whose open stage routes to you; no Add, no Create Bill.
  - new: Mounts the same ledger in review mode, titled Cost review, restricted to submissions whose open stage routes to you and that you did not submit yourself; no Add, no Create Bill.
  - evidence: packages/api/src/modules/audit/queue-filter.ts:51 "const excludeSelfSubmitted = REVIEWABLE_RESOURCES[resourceType].separationOfDuti"; packages/api/src/modules/audit/queue-filter.ts:52 "and s.submitted_by <> ${org.membership.id}"; apps/web/src/routes/_next/expenses/cost-lines.tsx:2330 "title={isReview ? "Cost review" : "Cost Detail"}"
- **op-21-009** set `/guide/20/pitfalls/0`
  - old: Create Bill refuses any line whose audit status is pending or rejected ('N selected line(s) are awaiting or failed cost review'); lines never submitted (null/draft) still bill.
  - new: Create Bill refuses any line whose review is pending, awaiting a withdrawal decision, or rejected ('N selected line(s) are awaiting or failed cost review'); lines never submitted (null/draft), withdrawn or approved still bill.
  - evidence: packages/api/src/routers/expense/cost-lines.ts:2429 "const OPEN_OR_FAILED = new Set(["pending", "withdraw_pending", "rejected"]);"; packages/api/src/routers/expense/cost-lines.ts:2433 "selected line(s) are awaiting or failed cost review"
- **op-21-010** set `/guide/20/pitfalls/2`
  - old: The Cost review queue shows only lines whose OPEN stage routes to you; a submitter who is not a reviewer sees an empty queue.
  - new: The Cost review queue shows only lines whose OPEN stage routes to you, and never lines you submitted, so **My submissions** and **My withdrawals** stay empty there. A submitter who is not a reviewer sees an empty queue.
  - evidence: packages/api/src/modules/audit/queue-filter.ts:52 "and s.submitted_by <> ${org.membership.id}"; apps/web/src/components/review-queue.tsx:191 "My submissions"; apps/web/src/components/review-queue.tsx:204 "My withdrawals"
- **op-21-003** insert `/guide/20/before/3`
  - new: Someone other than you can review the fee. If you are the only routed reviewer, submit is refused: **Nobody but you can approve this record. Ask an administrator to add a reviewer under Approval Process Setting.**
  - evidence: packages/api/src/modules/audit/submit.ts:173 "if (entry.separationOfDuties) {"; packages/api/src/modules/audit/submit.ts:199 "Nobody but you can approve this record. Ask an administrator to add a reviewer u"
- **op-21-011** insert `/guide/20/pitfalls/5`
  - new: A fee under review is frozen: it cannot be edited, removed or re-rated. Use **Review (n) > Request withdrawal**; it unfreezes once the withdrawal is approved.
  - evidence: packages/api/src/modules/expense/review-freeze.ts:43 "if (status === "under_review" || status === "withdrawal_under_review") {"; packages/api/src/routers/expense/cost-lines.ts:165 "await assertNotUnderReview(tx, ctx.org.organizationId, "cost_line", String(row.i"; apps/web/src/components/review-menu.tsx:55 "{ to: "withdraw_pending", label: "Request withdrawal" },"
- **op-21-012** insert `/guide/20/pitfalls/6`
  - new: Nobody decides their own fee: **Approve** or **Reject** from the ledger's Review menu on a line you submitted is refused with **You submitted this record. Another reviewer must decide it.**
  - evidence: apps/web/src/components/review-menu.tsx:51 "{ to: "approved", label: "Approve" },"; packages/api/src/modules/audit/decide.ts:61 "You submitted this record. Another reviewer must decide it."; packages/api/src/modules/audit/resources.ts:638 "separationOfDuties: true,"
- Re-anchored citations (auto): 3

## Step 22

- **op-22-008** set `/guide/21/before/1`
  - old: Lines are not pending or rejected in cost review (step 21).
  - new: Lines are not pending, awaiting withdrawal, or rejected in cost review (step 21).
  - evidence: packages/api/src/routers/expense/cost-lines.ts:2429 "const OPEN_OR_FAILED = new Set(["pending", "withdraw_pending", "rejected"]);"
- **op-22-009** set `/guide/21/pitfalls/1`
  - old: Any line pending or rejected in cost review blocks the whole request.
  - new: Any line pending, awaiting withdrawal or rejected in cost review blocks the whole request ('N selected line(s) are awaiting or failed cost review').
  - evidence: packages/api/src/routers/expense/cost-lines.ts:2429 ""withdraw_pending", "rejected""; packages/api/src/routers/expense/cost-lines.ts:2433 "selected line(s) are awaiting or failed cost review"
- **op-22-010** set `/guide/21/pitfalls/3`
  - old: Lines from different orders to the same settlement unit merge into one bill; the dialog has no per-order split.
  - new: Lines from different orders to the same settlement unit merge into one bill unless **Also split by order** is ticked.
  - evidence: apps/web/src/routes/_next/expenses/cost-lines.tsx:1240 "Also split by order"; packages/api/src/routers/expense/cost-lines.ts:2470 "input.groupByOrder ? (l.orderId ?? "") : """
- **op-22-011** set `/guide/21/pitfalls/4`
  - old: On the unticked path the opt-in Create bill review gate is checked against the (empty) tick list, so it does not apply there (cost-lines.ts:2502-2508 passes input.costLineIds).
  - new: If the lines of one bill name two different companies for one settlement unit, nothing is created ('… points at two different companies on the selected lines'). Correct the lines' Settlement Unit, then bill again.
  - evidence: packages/api/src/routers/expense/cost-lines.ts:2499 "points at two different companies on the selected lines. Correct the lines' Sett"; packages/api/src/routers/expense/cost-lines.ts:2446 "await assertGatesCleared(tx, organizationId, "cost_line", costLineIds, "create_b"
- **op-22-012** set `/guide/21/pitfalls/5`
  - old: The group key includes owning branch but the preview row text does not show it.
  - new: The group key includes owning branch; the preview row names the branch only when two rows differ by branch alone.
  - evidence: apps/web/src/routes/_next/expenses/cost-lines.tsx:1210 "{splitByBranchOnly ? ` · ${branchName(g.owningBranchId)}` : ""}"
- **op-22-013** set `/guide/21/writes/0`
  - old: One bill per group key (settlement unit, attribute, currency, owning branch, optional tax rate) with consecutive bill numbers, status open, totalAmount, settlement unit and settlementCompanyId copied from the first line, invoice title, remark.
  - new: One bill per group key (settlement unit, attribute, currency, owning branch, optional tax rate, optional order) with consecutive bill numbers, status open, totalAmount, settlement unit from the first line, invoice title, remark. settlementCompanyId comes from any line that names a company, else the company whose name exactly matches the settlement unit, else empty.
  - evidence: packages/api/src/routers/expense/cost-lines.ts:2470 "input.groupByOrder ? (l.orderId ?? "") : """; packages/api/src/routers/expense/cost-lines.ts:2492 "const picked = pickBucketCompany(bucketLines);"; packages/api/src/routers/expense/cost-lines.ts:2511 "eq(company.name, unit)"
- **op-22-014** insert `/guide/21/writes/1`
  - new: Due date: the bill copies its company's payment terms (days payable, counted after invoicing) and is due on its created date plus those days. A bill with no company, or a company with no terms, has no due date.
  - evidence: packages/db/src/migrations/0062_payment_terms_due_date.sql:150 "SELECT c."days_payable" INTO v_days"; packages/db/src/migrations/0062_payment_terms_due_date.sql:178 "NEW."due_date" := (coalesce(NEW."created_at", now()))::date + NEW."payment_terms"; packages/db/src/migrations/0062_payment_terms_due_date.sql:168 "NEW."payment_terms_anchor" := 'after_invoicing';"
- **op-22-015** insert `/guide/21/fields/1`
  - new: {"label":"Also split by order","required":false,"note":"Checkbox, default off. Also honoured when nothing is ticked."}
  - evidence: apps/web/src/routes/_next/expenses/cost-lines.tsx:1240 "Also split by order"; apps/web/src/routes/_next/expenses/cost-lines.tsx:1066 "const [groupByOrder, setGroupByOrder] = React.useState(false);"
- **op-22-016** insert `/guide/21/golden/5`
  - new: {"action":"Tick **Also split by order** if each order needs its own bill.","detail":"Off by default, so one bill can cover several orders to the same settlement unit (a month-end statement). Works on the unticked (全选) path too.","src":"apps/web/src/routes/_next/expenses/cost-lines.tsx:1240"}
  - evidence: apps/web/src/routes/_next/expenses/cost-lines.tsx:1240 "Also split by order"; apps/web/src/routes/_next/expenses/cost-lines.tsx:1231 "The server honours it on the 全选 path too"
- Re-anchored citations (auto): 7

## Step 23

- **op-23-001** set `/steps/22/what`
  - old: <strong>This gate has teeth — for invoicing and write-off.</strong> Invoicing is frozen until the bill's latest review passes, and unlike the template gate at step 4 this one ships switched on. It does not protect the bill’s own total: a line can still be removed, and the re-total that follows is not frozen.
  - new: <strong>This gate has teeth — for invoicing and write-off.</strong> Invoicing is frozen until the bill's latest review passes, and unlike the template gate at step 4 this one ships switched on. It also guards the bill itself: nothing on it saves while under review, and after approval only the paperwork (courier, voucher numbers, remark, tags) can change — removing a line is refused until the bill is dissolved and billed again.
  - evidence: packages/api/src/routers/expense/bills.ts:566 "gates: [underReviewGate("bill"), billSignedFieldsPostApproval, billFieldGates],"; packages/api/src/routers/expense/bills.ts:587 "gates: [underReviewGate("bill"), billTotalPostApprovalGate],"; packages/api/src/routers/expense/bills.ts:499 "was approved at this total. Dissolve it and bill the lines again to change them."
- **op-23-007** insert `/guide/22/pitfalls/4`
  - new: While a bill is under review nothing on it can be saved ('This record is under review and cannot be edited. Retract the submission first.'). After approval only **Courier**, **Courier No.**, **Financial Voucher No.**, **Exported Voucher Name**, **Remark** and **Bill Tag** still save; **Invoice Title**, **Service Fee**, **Service Fee Currency** and the exchange rate are refused, and removing a line asks you to dissolve the bill and bill the lines again.
  - evidence: packages/api/src/routers/expense/bills.ts:466 "export const BILL_SIGNED_FIELDS = ["; packages/api/src/routers/expense/bills.ts:469 ""serviceFeeCurrency","; packages/api/src/modules/audit/gates.ts:162 "This record is under review and cannot be edited. Retract the submission first."; apps/web/src/routes/_next/expenses/bills.tsx:459 "{field("courierNo", "Courier No.", "Tracking number")}"; apps/web/src/routes/_next/expenses/bills.tsx:466 "{field("financialVoucherNo", "Financial Voucher No.")}"
- Re-anchored citations (auto): 5

## Step 24

- **op-24-001** set `/steps/23/what`
  - old: Pick invoice category and type, optionally type the real tax-authority number, and accept or edit the amount — pre-filled with the outstanding balance, so <strong>partial invoicing works.</strong> The bill flips to invoiced once fully covered.
  - new: Pick invoice category and type, optionally type the real tax-authority number, and accept or edit the amount — pre-filled with the outstanding balance, so <strong>partial invoicing works.</strong> The bill flips to invoiced once fully covered; a written-off bill with a balance left can be invoiced too and goes to Done.
  - evidence: packages/api/src/routers/expense/bills.ts:1952 "if (b.status !== "open" && b.status !== "written_off") {"; packages/api/src/routers/expense/bills.ts:2215 "? ("done" as const)"
- **op-24-002** set `/steps/23/watch/html`
  - old: No credit check happens here. Scope, gate, currency, branch, attribute and status are all checked; the counterparty's outstanding exposure never is.
  - new: Over-limit invoicing needs a reason. When a covered bill's company is over its credit limit, the dialog names it and <strong>Issue Invoice</strong> stays disabled until a reason of at least 10 characters is typed; the reason goes to the audit log. A bill with no company, or a company with no limit, is never checked.
  - evidence: apps/web/src/routes/_next/expenses/bills.tsx:644 "Reason for invoicing over the limit"; apps/web/src/routes/_next/expenses/bills.tsx:579 "const reasonOk = creditOverrideReason.trim().length >= 10;"; packages/api/src/routers/expense/bills.ts:2120 "if (exposure.limit === null) continue; // no limit set — nothing to enforce"; packages/api/src/routers/expense/bills.ts:2333 "action: "bill.invoice.credit_override","
- **op-24-003** set `/guide/23/before/0`
  - old: Bill status is open (partially invoiced bills stay open).
  - new: Bill status is open, or written off with a balance left (a partially invoiced bill keeps its status).
  - evidence: apps/web/src/lib/expense.ts:135 "return bill.status === "written_off" && invoiceOutstanding(bill) > 0;"
- **op-24-006** set `/guide/23/golden/1/detail`
  - old: Disabled unless the bill status is open. Opens **Invoicing — <bill no>**.
  - new: Enabled while the bill is open, or written off with a balance left. Opens **Invoicing — <bill no>**.
  - evidence: apps/web/src/routes/_next/expenses/-bills.columns.tsx:832 "disabled={!isInvoiceable(row.original)}"; apps/web/src/lib/expense.ts:135 "return bill.status === "written_off" && invoiceOutstanding(bill) > 0;"; apps/web/src/routes/_next/expenses/bills.tsx:599 "<DialogTitle>Invoicing — {bill?.billNo}</DialogTitle>"
- **op-24-010** set `/guide/23/golden/4/detail`
  - old: Placeholder 'Auto-generated if left blank'.
  - new: Placeholder 'Auto-generated if left blank'. A number in the system's own NCT-INV-… shape, or one already used, is refused and the dialog stays open.
  - evidence: packages/api/src/routers/expense/bills.ts:1833 ""NCT-INV-… numbers are generated by the system. Leave Invoice Number blank to ge"; packages/api/src/routers/expense/bills.ts:2184 "is already used in this organisation."; apps/web/src/routes/_next/expenses/bills.tsx:593 "onError: (error: Error) => toast.error(error.message),"
- **op-24-012** set `/guide/23/golden/5/detail`
  - old: Editable.
  - new: Editable. What you type becomes this invoice's buyer; the bill's own title is not changed.
  - evidence: packages/api/src/routers/expense/bills.ts:1760 "The dialog's Invoice Title. It becomes THIS invoice's `buyer`"; packages/api/src/routers/expense/bills.ts:1761 "the bill's own `invoice_title` is not written."
- **op-24-015** set `/guide/23/golden/7/detail`
  - old: Disabled until Category and Type are chosen. Toast 'Invoice <no> issued'. A full amount flips the bill to invoiced; a partial one leaves it open.
  - new: Disabled until Category and Type are chosen (and, over a credit limit, until a reason is typed). Toast 'Invoice <no> issued'. A full amount flips an open bill to invoiced and a written-off bill to done; a partial one leaves the status as it was.
  - evidence: apps/web/src/routes/_next/expenses/bills.tsx:749 "(needsReason && !reasonOk)"; apps/web/src/routes/_next/expenses/bills.tsx:586 "toast.success(`Invoice ${result.invoice.invoiceNumber} issued`);"; packages/api/src/routers/expense/bills.ts:2215 "? ("done" as const)"; packages/api/src/routers/expense/bills.ts:2217 ": readStatus;"
- **op-24-016** set `/guide/23/fields/3/note`
  - old: Defaults to bill invoice title.
  - new: Defaults to the bill invoice title; becomes the invoice's buyer, the bill is not changed.
  - evidence: packages/api/src/routers/expense/bills.ts:1760 "It becomes THIS invoice's `buyer`"
- **op-24-017** set `/guide/23/writes/1`
  - old: bill.invoicedAmount increased; bill.status invoiced when fully covered.
  - new: bill.invoicedAmount increased; when fully covered bill.status becomes invoiced (or done, if it was written off).
  - evidence: packages/api/src/routers/expense/bills.ts:2213 "const nextStatus = p.fullyInvoiced"; packages/api/src/routers/expense/bills.ts:2215 "? ("done" as const)"
- **op-24-018** set `/guide/23/pitfalls/1`
  - old: A bill that is invoiced or written off cannot be invoiced again ('is not invoiceable'); the Invoicing icon is greyed.
  - new: A bill that is invoiced or done cannot be invoiced again ('is not invoiceable'); the Invoicing icon is greyed. A written-off bill with nothing left to invoice is greyed too.
  - evidence: packages/api/src/routers/expense/bills.ts:1954 "is not invoiceable (status: ${b.status})"; apps/web/src/lib/expense.ts:135 "return bill.status === "written_off" && invoiceOutstanding(bill) > 0;"
- **op-24-019** set `/guide/23/pitfalls/3`
  - old: No credit-limit or customer credit check exists on this path.
  - new: Over a company's credit limit the server refuses without a reason ('… is over its credit limit … Give a reason to invoice anyway.'). Bills with no company are not checked.
  - evidence: packages/api/src/routers/expense/bills.ts:2140 "message: `${sentence}. Give a reason to invoice anyway.`,"; packages/api/src/routers/expense/bills.ts:2076 "A bill with no settlement company contributes no check:"
- **op-24-020** insert `/guide/23/golden/7`
  - new: {"action":"If a notice says the company is over its credit limit, type a **Reason for invoicing over the limit**.","detail":"At least 10 characters; kept in the audit log. Figures in another currency and bills with no company are listed as not counted.","src":"apps/web/src/routes/_next/expenses/bills.tsx:644"}
  - evidence: apps/web/src/routes/_next/expenses/bills.tsx:644 "Reason for invoicing over the limit"; apps/web/src/routes/_next/expenses/bills.tsx:650 "At least 10 characters — kept in the audit log."
- **op-24-021** insert `/guide/23/fields/5`
  - new: {"label":"Reason for invoicing over the limit","required":false,"note":"Shown and required (10+ characters) only when a covered company is over its credit limit."}
  - evidence: apps/web/src/routes/_next/expenses/bills.tsx:644 "Reason for invoicing over the limit"; apps/web/src/routes/_next/expenses/bills.tsx:579 "const reasonOk = creditOverrideReason.trim().length >= 10;"
- **op-24-022** insert `/guide/23/writes/3`
  - new: An over-limit override writes a bill.invoice.credit_override audit row on the company, with the reason.
  - evidence: packages/api/src/routers/expense/bills.ts:2333 "action: "bill.invoice.credit_override","; packages/api/src/routers/expense/bills.ts:2344 "reason: input.creditOverrideReason,"
- Re-anchored citations (auto): 8

## Step 25

- **op-25-001** set `/steps/24/what`
  - old: Preview the real document — seller (name, address, phone, contact — never a tax ID) and buyer (name, address, tax ID), fee lines with tax rate, two totals, payment terms derived from the company's days-payable — then save it as PDF or XLSX, which marks it downloaded. A void reverses the whole allocation atomically and makes the bill re-invoiceable.
  - new: Preview the real document — seller (name, address, phone, contact — never a tax ID) and buyer (name, address, tax ID), fee lines with tax rate, two totals, payment terms from the bill's own term (the customer's current term only when no covered bill has one) — then save it as PDF or XLSX; the server records it as downloaded. A void reverses the whole allocation atomically and makes the bill re-invoiceable.
  - evidence: packages/api/src/modules/export/invoice-document.ts:444 "the covered bills' 0062 snapshots that are set"; packages/api/src/modules/export/invoice-document.ts:445 "none set → the company's term as it reads today"; packages/api/src/routers/expense/invoices.ts:550 "Step 25 D5-A: Downloaded is recorded HERE, in the request that produced"
- **op-25-002** set `/guide/24/golden/0/detail`
  - old: Stats tiles, all page-scoped: **Total Invoices**, **Active (this page)**, **Cancelled (this page)**, **Downloaded (this page)**.
  - new: Stats tiles, all page-scoped except the first: **Total Invoices**, **Issued (this page)**, **Cancelled (this page)**, **Downloaded (this page)**.
  - evidence: apps/web/src/routes/_next/expenses/invoices.tsx:945 "label: "Issued (this page)""; apps/web/src/routes/_next/expenses/invoices.tsx:944 "label: "Total Invoices", value: total"
- **op-25-005** set `/guide/24/golden/3/detail`
  - old: Saves the file, toast 'Saved <filename>', and marks the invoice Downloaded the first time.
  - new: Saves the file, toast 'Saved <filename>'. The server marks the invoice Downloaded in the same request, the first time only, and only for staff with expense.invoice.update.
  - evidence: apps/web/src/routes/_next/expenses/invoices.tsx:327 "toast.success(`Saved ${file.filename}`);"; packages/api/src/routers/expense/invoices.ts:570 "if (!permittedNode(context.org, EXPENSE.invoiceUpdate)) return;"; packages/api/src/routers/expense/invoices.ts:569 "if (document.header.isDownloaded) return;"
- **op-25-010** set `/guide/24/golden/7/detail`
  - old: Bill reverts to Open and its cost lines become uninvoiced so it can be re-invoiced.
  - new: The bill goes back to Open (or stays written off if its lines were already fully settled) and its cost lines become uninvoiced, so it can be invoiced again.
  - evidence: packages/api/src/routers/expense/invoices.ts:871 ".set(allWritten ? { status: "written_off" } : { status: "open", writeOffTime: nu"; packages/api/src/routers/expense/invoices.ts:859 "off reads `written_off` (keep writeOffTime); a `done` bill voided"
- **op-25-012** set `/guide/24/writes/0`
  - old: invoice.isDownloaded set true on first successful save (not re-stamped).
  - new: invoice.isDownloaded set true by the server in the request that produces the file — first time only (not re-stamped), and only for holders of expense.invoice.update.
  - evidence: packages/api/src/routers/expense/invoices.ts:570 "EXPENSE.invoiceUpdate"; packages/api/src/routers/expense/invoices.ts:576 "(row) => (row.isDownloaded === true ? null : { isDownloaded: true }),"
- **op-25-016** shot `/guide/24/shots/0` — needs-human: capture needed: stats tile renamed from Active (this page) to Issued (this page)
- **op-25-013** insert `/guide/24/writes/4`
  - new: An audit row (expense.invoice.exportDocument) for every file produced, naming who took it and the format.
  - evidence: packages/api/src/routers/expense/invoices.ts:535 "action: "expense.invoice.exportDocument""
- **op-25-014** insert `/guide/24/pitfalls/4`
  - new: A red notice above the document warns when the covered bills carry different payment terms (the shortest is printed) or none (the customer's current terms are printed). These warnings are for the screen only; they never go into the file.
  - evidence: packages/api/src/modules/export/invoice-document.ts:481 "The covered bills carry different payment terms"; packages/api/src/modules/export/invoice-document.ts:490 "No covered bill carries a payment term of its own"; apps/web/src/routes/_next/expenses/invoices.tsx:459 "they are never written into the file"
- **op-25-015** insert `/guide/24/pitfalls/5`
  - new: If the buyer's details are hidden from you, **下载 XLSX** and **下载 PDF** are disabled; ask a colleague who can see the buyer.
  - evidence: apps/web/src/routes/_next/expenses/invoices.tsx:452 "The buyer's details are hidden from you, so you cannot download this document."; apps/web/src/routes/_next/expenses/invoices.tsx:429 "disabled={saving !== null || !documentQuery.data || buyerHidden}"; packages/api/src/routers/expense/invoices.ts:522 "Ask a colleague who can."
- Re-anchored citations (auto): 7

## Step 26

- **op-26-001** set `/steps/25/what`
  - old: Record the receipt — direction, settlement party, account, payment way, currency, amount, bank slip, date — then verify it against a bill, or, from the same dialog, a single billed cost line. There is no exchange-rate field. The amount spreads forward across that bill's cost lines in creation order. A wrong write-off is reversed, not edited.
  - new: Record the receipt — direction, settlement party, account, payment way, currency, amount, an exchange rate when the currency differs from the reporting one, bank slip, date — then verify it against a bill on its own side (a receipt settles receivables, a payment payables), or, from the same dialog, a single billed cost line. The amount spreads forward across that bill's cost lines in creation order. A payment can be edited while nothing live is settled against it; a wrong write-off is voided with a reason, not edited.
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:578 "Exchange Rate ({currency} → {toCurrency})"; packages/api/src/routers/expense/write-offs.ts:411 "settles ${paySide} bills only."; apps/web/src/routes/_next/expenses/payments.tsx:1113 "Step 26 D6-A: editable while nothing live is settled against it."; packages/api/src/routers/expense/write-offs.ts:1069 "reason: z.string().trim().min(1, "Give a reason for voiding").max(500),"
- **op-26-002** set `/guide/25/role`
  - old: Cashier / Accounting · the sidebar item needs expense:read, but the lists gate on expense.payment.read and expense.writeOff.read; expense.payment.create to record, expense.writeOff.verify to verify, expense.writeOff.reverse to void, expense.payment.delete to delete an unsettled payment.
  - new: Cashier / Accounting · the sidebar item needs expense:read, but the lists gate on expense.payment.read and expense.writeOff.read; expense.payment.create to record, expense.payment.update to edit, expense.writeOff.verify to verify, expense.writeOff.reverse to void, expense.payment.delete to delete an unsettled payment.
  - evidence: packages/api/src/routers/expense/payments.ts:449 ".use(requireNode(EXPENSE.paymentUpdate))"; packages/api/src/modules/expense/permissions.ts:52 "paymentUpdate: "expense.payment.update""
- **op-26-008** set `/guide/25/golden/5/detail`
  - old: Opens **Verify — <payment no>**.
  - new: Opens **Verify — <payment no>**. Disabled once the payment is fully written off.
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:799 "<DialogTitle>Verify — {payment?.paymentNo}</DialogTitle>"; apps/web/src/routes/_next/expenses/payments.tsx:1126 "(Number(row.original.writtenOffAmount) || 0) >= (Number(row.original.amount) || "
- **op-26-010** set `/guide/25/golden/6/detail`
  - old: Options show number and currency; only billed cost lines are offered.
  - new: Options show number and currency. Only this payment's own side is offered (receivables for a Receipt, payables for a Payment), only billed cost lines, and nothing already fully settled.
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:706 "const side: "receivable" | "payable" | undefined = payment"; apps/web/src/routes/_next/expenses/payments.tsx:741 ".filter((b) => b.status !== "written_off" && b.status !== "done");"; apps/web/src/routes/_next/expenses/payments.tsx:752 ".filter((l) => l.billId && (Number(l.writtenOffAmount) || 0) < (Number(l.amount)"
- **op-26-015** set `/guide/25/golden/10/action`
  - old: To undo, go to **Money > Write-offs**, press **Void write-off** on the row, then **Void Write-off**.
  - new: To undo, go to **Money > Write-offs**, press **Void write-off** on the row, type a **Reason**, then press **Void Write-off**.
  - evidence: apps/web/src/routes/_next/expenses/write-offs.tsx:202 "title="Void write-off""; apps/web/src/routes/_next/expenses/write-offs.tsx:218 "Reason <span className="text-destructive">*</span>"; apps/web/src/routes/_next/expenses/write-offs.tsx:237 "{busy ? "Working…" : "Void Write-off"}"
- **op-26-017** set `/guide/25/writes/0`
  - old: payment row (paymentNo, direction, settlement unit, accounts, way, currency, amount, slip number, dates, note).
  - new: payment row (paymentNo, direction, settlement unit, accounts, way, currency, amount, exchange rate, slip number, dates, note).
  - evidence: packages/api/src/routers/expense/payments.ts:444 "`exchangeRate: null` stores the column's default of 1"; apps/web/src/routes/_next/expenses/payments.tsx:287 "const rate = showRateRef.current ? value.exchangeRate.trim() : "";"
- **op-26-018** set `/guide/25/writes/3`
  - old: Void: split returned to payment and lines, bill status reverted, write-off marked voided.
  - new: Void: split returned to payment and lines, bill status reverted, write-off marked voided with its reason.
  - evidence: packages/api/src/routers/expense/write-offs.ts:1174 "voidReason: input.reason,"
- **op-26-019** set `/guide/25/pitfalls/4`
  - old: A payment that has been written off cannot be deleted; void the write-off first.
  - new: A payment that has ever been written off cannot be deleted, even after the write-off is voided; correct it with **Edit** instead.
  - evidence: packages/api/src/routers/expense/payments.ts:151 "those are a permanent record and deleting the payment would destroy them"; apps/web/src/routes/_next/expenses/payments.tsx:1144 "A payment with voided write-offs cannot be deleted; edit it instead."
- **op-26-025** shot `/guide/25/shots/0` — needs-human: capture needed: payment rows gained an Edit (pencil) action; Verify is disabled on fully written-off rows
- **op-26-020** insert `/guide/25/pitfalls/6`
  - new: A receipt settles receivable bills only and a payment payable bills only; the other side is refused.
  - evidence: packages/api/src/routers/expense/write-offs.ts:411 "settles ${paySide} bills only. ${targetLabel} is ${attribute}."
- **op-26-021** insert `/guide/25/pitfalls/7`
  - new: **Edit** on a payment is disabled while anything live is settled against it; void its write-off on **Money > Write-offs** first.
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:1115 "disabled={(Number(row.original.writtenOffAmount) || 0) > 0}"; packages/api/src/routers/expense/payments.ts:180 "This payment has a live write-off. Void it on Write-offs first."
- **op-26-022** insert `/guide/25/fields/9`
  - new: {"label":"Exchange Rate","required":false,"note":"Shown only when the currency differs from the reporting currency; prefilled from the write-off rate on file. Blank = no rate."}
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:578 "Exchange Rate ({currency} → {toCurrency})"; apps/web/src/routes/_next/expenses/payments.tsx:587 "Prefilled from the write-off rate on file. Correct it to the bank's rate."
- **op-26-023** insert `/guide/25/fields/17`
  - new: {"label":"Reason","required":true,"note":"Void write-off dialog; max 500 chars."}
  - evidence: apps/web/src/routes/_next/expenses/write-offs.tsx:218 "Reason <span className="text-destructive">*</span>"; apps/web/src/routes/_next/expenses/write-offs.tsx:223 "maxLength={500}"
- **op-26-024** insert `/guide/25/golden/4`
  - new: {"action":"If an **Exchange Rate** row appears, correct the prefilled write-off rate to the bank's rate, or leave it blank.","detail":"Shown only when the currency differs from the reporting currency. Blank means no rate, so no exchange gain or loss is booked when it is settled.","src":"apps/web/src/routes/_next/expenses/payments.tsx:578"}
  - evidence: apps/web/src/routes/_next/expenses/payments.tsx:578 "Exchange Rate ({currency} → {toCurrency})"; apps/web/src/routes/_next/expenses/payments.tsx:781 "No exchange gain or loss booked: the payment has no exchange rate."
- Re-anchored citations (auto): 11

## Step 27

- **op-27-001** set `/steps/26/what`
  - old: Tick cost lines and export the customer statement, the debit note or the per-shipment profit sheet. Then work the five Financial Stat tabs: profit analysis, profit statement, receipts and payments, billing records, and ageing with days overdue (credit limit appears only in the ageing export).
  - new: Tick cost lines and export the customer statement or debit note (one settlement unit each) or the per-shipment profit sheet. Then work the five Financial Stat tabs, whose months follow the branch's time zone: profit analysis, profit statement, receipts and payments, billing records, and ageing with days overdue plus a separate line for bills with no due date (credit limit appears only in the ageing export).
  - evidence: packages/api/src/routers/export.ts:186 "goes to one settlement unit. This selection has"; packages/api/src/routers/report.ts:422 "zone — the one day function the month-end pack and every Financial Stat tab"; apps/web/src/routes/_next/report/financial/index.tsx:2389 "{undatedBillsNotice(ageing.data.undated)}"
- **op-27-006** set `/guide/26/golden/5/detail`
  - old: Exports that order's saved fees.
  - new: Exports that order's saved fees. For a statement or DEBIT_NOTE over fees of several parties, pick **结算单位 · Settlement unit** first.
  - evidence: apps/web/src/routes/_next/order.$orderId.expenses.tsx:935 "lines={costs}"; apps/web/src/components/export-table-dialog.tsx:392 "结算单位 · Settlement unit <span"
- **op-27-008** set `/guide/26/golden/6/detail`
  - old: Tabs: **Profit analysis statement**, **Profit statement**, **Actual receipt and payment report**, **Billing records**, **Ageing report**.
  - new: Tabs: **Profit analysis statement**, **Profit statement**, **Actual receipt and payment report**, **Billing records**, **Ageing report**. Months are read in the branch's time zone; profit figures are labelled **At each bill's rate · billed fees only**.
  - evidence: apps/web/src/routes/_next/report/financial/index.tsx:993 "label: "Profit analysis statement""; apps/web/src/routes/_next/report/financial/index.tsx:997 "label: "Ageing report""; packages/api/src/modules/report/summary-notices.ts:78 "At each bill's rate · billed fees only"; packages/api/src/routers/report.ts:422 "every Financial Stat tab"
- **op-27-013** set `/guide/26/pitfalls/0`
  - old: There is no period close or month lock in NCT; the month-end pack is an export only (packages/api/src/routers/report.ts:2683).
  - new: There is no period close or month lock in NCT; the month-end pack is an export only (packages/api/src/routers/report.ts:2978). Locking is per job: **Lock job** on an order's **Lifecycle** menu stops its fees being added or changed.
  - evidence: packages/api/src/routers/report.ts:2978 "exportMonthEndPack: orgProcedure"; apps/web/src/components/order-ledger/order-row-actions.tsx:267 "Its fees cannot be added or changed until the job is unlocked."
- **op-27-020** shot `/guide/26/shots/0` — needs-human: capture needed: Financial Stat header gained the 'Statements & debit notes for <month>' link; profit tiles carry a basis caption; Ageing tab may show the undated line
- **op-27-021** shot `/guide/26/shots/1` — needs-human: capture needed: Business Stat Receivable (local) tile now carries the caption 'At each fee's own rate · all fees'
- **op-27-014** insert `/guide/26/pitfalls/6`
  - new: The Financial Stat **PDF** prints one line per currency, with no cross-currency sum. A converted total prints only when every bill converts into one named currency (the base currency, or a single branch currency).
  - evidence: packages/api/src/modules/report/pdf.ts:251 "and printed with no unit. Now: one line pair per currency, then the local"; packages/api/src/modules/report/pdf.ts:252 "roll-up only under a NAMED currency"
- **op-27-015** insert `/guide/26/pitfalls/7`
  - new: Open bills with no due date sit outside the Ageing buckets, on a separate line: '<n> open bills (…) have no due date and cannot show days overdue.'
  - evidence: packages/api/src/modules/report/summary-notices.ts:63 "have no due date and cannot show days overdue."; apps/web/src/routes/_next/report/financial/index.tsx:2389 "{undatedBillsNotice(ageing.data.undated)}"
- **op-27-016** insert `/guide/26/pitfalls/8`
  - new: **Statements & debit notes for <month>** on Financial Stat opens Cost lines filtered by Fee Time in whole UTC days, while the pack and tabs use bill and payment days in the branch's time zone, so a fee entered near midnight can fall in another month.
  - evidence: apps/web/src/routes/_next/report/financial/index.tsx:975 "Statements &amp; debit notes for {monthEndMonth}"; apps/web/src/routes/_next/report/financial/index.tsx:966 "whole days in UTC. The Month-end pack and these tabs use bill and payment days i"
- **op-27-017** insert `/guide/26/pitfalls/9`
  - new: Ending or locking a job (**End job**, **Cancel end**, **Lock job**, **Unlock job** on the order ledger row's **Lifecycle** menu) is not an accounting action: accounting does not see the menu. Ask ops or a branch manager.
  - evidence: apps/web/src/components/order-ledger/order-row-actions.tsx:256 "label: "Cancel end""; apps/web/src/components/order-ledger/order-row-actions.tsx:335 "aria-label="Lifecycle""; apps/web/src/components/order-ledger/order-row-actions.tsx:119 "Accounting holds no `update`, so it sees no"; apps/web/src/components/order-ledger/order-row-actions.tsx:246 "Ask ops or a branch manager."
- **op-27-018** insert `/guide/26/fields/5`
  - new: {"label":"结算单位 · Settlement unit","required":false,"note":"Shown for 对账单 / DEBIT_NOTE only when the ticked lines span several units; then required."}
  - evidence: apps/web/src/components/export-table-dialog.tsx:392 "结算单位 · Settlement unit <span"; apps/web/src/components/export-table-dialog.tsx:207 "const needsUnit = ONE_PARTY_TYPES.has(type) && units.length > 1;"
- **op-27-019** insert `/guide/26/golden/3`
  - new: {"action":"For **对账单** or **DEBIT_NOTE** over ticked lines from several parties, pick **结算单位 · Settlement unit**.","detail":"One document goes to one settlement unit; only that unit's lines are sent. With nothing ticked, filter to one settlement unit first or the export is refused.","src":"apps/web/src/components/export-table-dialog.tsx:392"}
  - evidence: apps/web/src/components/export-table-dialog.tsx:392 "结算单位 · Settlement unit <span"; apps/web/src/components/export-table-dialog.tsx:414 "goes to one settlement unit."; packages/api/src/routers/export.ts:186 "Filter or pick one."
- Re-anchored citations (auto): 9

## Needs a human (not applied)

- Step 16 `/guide/15/fixes/0`: Fix 'Released or On-hold from a dropdown' looks fixed (Status is now draft/confirmed/cancelled; server refuses others). run /zyt-audit (packages/api/src/modules/lading/status.ts:13)
- Step 16 `/guide/15/fixes/1`: Fix 'B/L under review stays fully editable' looks fixed (update, delete and deleteAttachment call assertNotUnderReview). run /zyt-audit (packages/api/src/routers/lading.ts:1693)
- Step 16 `/guide/15/fixes/2`: Fix 'OCR validation issues thrown away' looks fixed (issues shown per field; errors block Apply). run /zyt-audit (apps/web/src/routes/_next/lading/create.tsx:1676)
- Step 16 `/guide/15/fixes/3`: Fix 'linking the order carries only its job number' looks fixed (prefillFromOrder fills blank fields). run /zyt-audit (apps/web/src/routes/_next/lading/create.tsx:567)
- Step 17 `/ledger`: Any ledger item saying no screen can submit a lading for review now looks stale. run /zyt-audit (apps/web/src/routes/_next/lading/$id/index.tsx:453)
- Step 18 `/ledger`: Any ledger item saying paperwork cannot be linked to its order (only 'quote' links) now looks stale. run /zyt-audit (packages/api/src/routers/document.ts:109)
- Step 16 `/guide/15/shots/0`: Default create-form view looks unchanged; Status dropdown lost Released/On-hold, OCR tab now shows error/warning findings, and fields opened from an order show 'from order'. Recapture optional (apps/web/src/routes/_next/lading/create.tsx:168)
- Step 18 `/guide/17/golden/3`: Pre-existing drift, not from this release: on a needs-review document edits save via 'Save corrections & approve' (toast 'Corrections saved and approved'); 'Save amendment' exists only after approval. Consider rewording (apps/web/src/routes/_next/documents.$documentId.tsx:609)
- Step 17 `/guide/16/golden/8`: The approvals-queue name for B/L review was not checked in the nav; the inserted text avoids naming it (route /approve/lading) (apps/web/src/routes/_next/approve/lading.tsx:13)
- Step 19 `/guide/18/shots/0`: Board shot may be stale: where no MYCIEDS/SBCP login is saved an amber "checks are off" notice now sits above the grid, and BL Number renders as an underlined link. Whether production shows the notice depends on saved logins (release note: last portal check 2026-07-28). Recapture if it shows. (apps/web/src/routes/_next/customs-tracking.tsx:193)
- Step 19 `/guide/18/shots/1`: Overview gate-out card now shows a "Checks off — no … login" line when logins are missing; recapture if production shows it. (apps/web/src/components/shell-next/card-gateout.tsx:292)
- Step 19 `/ledger/items (island)`: Ledger item "The operational automation is an island" may be partly stale: jobs now link to Order and Fees, and the order/B/L card shows the countdown. run /zyt-audit (apps/web/src/components/customs-tracking/bl-job-record.tsx:267)
- Step 20 `/guide/19/fixes/0`: Fix "Unnamed fee rows are dropped without a word" looks fixed: a nameless row with content or a saved unnamed fee now refuses the Save by row. run /zyt-audit (apps/web/src/lib/fee-rows.ts:128)
- Step 20 `/guide/19/shots/0`: Expense Entry shot probably still accurate (new rows show "From rate table" in Exchange rate; frozen rows disabled; payable no-vendor warning only when blank). Optional recapture. (apps/web/src/routes/_next/order.$orderId.expenses.tsx:496)
- Step 21 `/guide/20/fixes/1`: Fix "An approved fee's exchange rate can still be rewritten in bulk" looks fixed: exchangeRateBatch now skips under_review and approved-and-locked fees. run /zyt-audit (packages/api/src/routers/expense/batch.ts:174)
- Step 21 `/guide/20/fixes/2`: Fix "An approved fee can be deleted outright" looks fixed: batch delete applies costLineReviewFreezes (under review / approved) with reviewFreezeSentence(kind, "deleted"). run /zyt-audit (packages/api/src/routers/expense/cost-lines.ts:1653)
- Step 21 `/guide/20/fixes/3`: Fix "A fee sitting in the review queue is fully editable" looks fixed: the cost-line writer now calls assertNotUnderReview, and saveChildren refuses under-review changes. run /zyt-audit (packages/api/src/routers/expense/cost-lines.ts:165)
- Step 21 `/guide/20/fixes/0`: Fix "fees must pass review before billing gate is silently skipped when nothing is ticked" not re-verified here; the gate now reads the locked resolved rows. run /zyt-audit (packages/api/src/routers/expense/cost-lines.ts:2433)
- Step 21 `/steps/20/actors`: Separation of duties now enforced (submitter cannot decide their own fee; submit refused if no other reviewer). Actors already list operations (does) and reviewer (approves) as separate roles, so no actors op drafted; confirm whether the page wants an explicit "not the submitter" marker. (packages/api/src/modules/audit/resources.ts:638)
- Step 22 `/guide/21/fixes/0`: fix "Bills cannot be split per order" looks fixed: the dialog has Also split by order, sent as groupByOrder. run /zyt-audit (apps/web/src/routes/_next/expenses/cost-lines.tsx:1240)
- Step 22 `/guide/21/shots/0`: Shot shows the Cost Detail ledger; the Create Bill dialog gained Also split by order but the ledger itself looks unchanged. Recapture only if the shot shows the dialog. (apps/web/src/routes/_next/expenses/cost-lines.tsx:1240)
- Step 24 `/guide/23/shots/0`: Bill Management ledger shot; the Invoicing icon is now enabled on written-off bills with a balance, and the dialog has a credit-limit notice. Recapture only if the shot shows the dialog or a written-off row. (apps/web/src/routes/_next/expenses/-bills.columns.tsx:832)
- Step 23 `/guide/22/pitfalls/1`: Pitfall prose cites gates.ts:78-85; the no-flow early return is now packages/api/src/modules/audit/gates.ts:77-78 and the gate lookup 80-84. Not a src field, left alone. (packages/api/src/modules/audit/gates.ts:78)
- Step 23 `/steps/22/what`: The ledger row Review menu still lists Approve / Reject / Approve withdrawal next to Submit for review (MOVES is unfiltered). The release note says bill review happens only in the queues. Decide whether the SOP should say so; the engine still requires expense.bill.review to decide. (apps/web/src/components/review-menu.tsx:50)
- Step 27 `/guide/26/fixes/0`: Fix 'The month-end documents have no menu entry' may be partly addressed: Financial Stat now has a 'Statements & debit notes for <month>' link that opens Cost lines filtered to the month (not a Reports menu entry). run /zyt-audit (apps/web/src/routes/_next/report/financial/index.tsx:975)
- Step 27 `/steps/26/watch/html`: Watch says the three documents 'open from an export dialog on two screens'. Still true, but Financial Stat now links to Cost lines for the picked month; consider adding that to the watch text. (apps/web/src/routes/_next/report/financial/index.tsx:975)
- Step 25 `/guide/24/golden/7`: Release note says the invoice-void confirm text mentions the bill staying Written off; the code's confirm description does not ('The amount it invoiced goes back to its bill, so the bill can be invoiced again.'). SOP drafted from code; release note is wrong or a later commit is missing. (apps/web/src/routes/_next/expenses/invoices.tsx:801)
- Step 26 `/guide/25/shots/1`: Write-offs list has a new 'Void reason' column; check whether it is visible by default before deciding a recapture of step-26-writeoffs.jpg. (apps/web/src/routes/_next/expenses/write-offs.tsx:369)
