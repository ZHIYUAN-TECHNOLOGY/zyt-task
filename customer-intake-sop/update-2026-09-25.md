# SOP update - ZYT-Task/customer-intake-sop - 2026-09-25

- Tier: light
- Source: all-light
- Counts: auto 8 / confirm 15 / needs-human 12 (3 needs-human ops + 9 needs-human notes without ops)
- Pending changeset: C:/Project/ZYT-Task/customer-intake-sop/.zyt/pending-2026-09-25.json
- Evidence check: C:/Project/ZYT-Task/customer-intake-sop/.zyt/pending-2026-09-25.verified.json (26 kept, 0 lines rewritten, 0 rejected)

Sync only: descriptive drift. System-correctness checks (rules against code, defects, ledger status) are /zyt-audit.

## Rule conflicts -> proposed ledger items

Rule-violation ops: None. (sop.json has no rules[], so no rule-violation ops were produced.)

Gate/role/permission findings with no matching rule: None.

## Chain changes

Apply order matters because removals shift indexes. Remove /breaks/3 (op-015) before /breaks/2 (op-005). The findings asked for /breaks/6 to go first, but op-024 now rewrites /breaks/6 instead of removing it, so it does not shift indexes. Apply it before the removals so its path still points at the same break.

1. **After step 13, "Intake queue → the job"** - op-005, remove, confirm
   - SOP says: the queue has no exit; there is no link out of the screen to open the order.
   - Code does: the detail pane and the row overflow both offer **Open full page**.
   - Evidence: `apps/web/src/routes/_next/order.to-receive.tsx:1865` "Open full page"; `apps/web/src/routes/_next/order.to-receive.tsx:496` "Open full page"

2. **After step 15, "Order → Bill of lading"** - op-015, remove, confirm
   - SOP says: there is no route from the job to its B/L; the bill has to find its order again in a 200-row dropdown.
   - Code does: the order record page links to `/lading/create` with the orderId prefilled ("Raise bill of lading").
   - Evidence: `apps/web/src/components/order-ledger/order-record-page.tsx:149` `<Link to="/lading/create" search={{ orderId }}>`; `apps/web/src/components/order-ledger/order-record-page.tsx:152` "Raise bill of lading"
   - Note: this is the page's only `lead: true` break. A human must pick a new lead break.

3. **After step 21, "Order → Bill"** (/breaks/6/body) - op-024, set, needs-human (the validator also warns it is not structural, so it should be chain:false)
   - SOP says: the order's expense page has no Create bill, and its link to the fee ledger has no filter.
   - Code does: the Cost Detail link now carries `orderId` and `status: "bill_not_established"`. There is still no Create bill on the order page.
   - Proposed: rewrite the body, not remove the break.
   - Evidence: `apps/web/src/routes/_next/order.$orderId.expenses.tsx:1077` `search={{ orderId, status: "bill_not_established" }}`

4. **Chain aria label** (/chain/ariaLabel) - op-025, set, needs-human
   - SOP says: "Eight hand-offs are severed."
   - Proposed: "Six hand-offs are severed." This is correct only if op-005 and op-015 are both applied and /breaks/6 is kept (op-024 rewrites it). The run notes say "Five", which assumed /breaks/6 would be removed. Use "Six" unless /breaks/6 is dropped.
   - Evidence: `apps/web/src/components/order-ledger/order-record-page.tsx:152` "Raise bill of lading"; `apps/web/src/routes/_next/order.$orderId.expenses.tsx:1077` `search={{ orderId, status: "bill_not_established" }}`

## Content changes

1. **Step 13, golden 4 src** - op-001, reanchor, auto
   - SOP says: `order.to-receive.tsx:764`. Code does: the same toast code is now at :875.
   - Evidence: `apps/web/src/routes/_next/order.to-receive.tsx:875` ": `${done} order${done > 1 ? "s" : ""} ${verb}`,"; `:868` `const verb = mode === "receive" ? "received" : ...`

2. **Step 13, pitfall 1** - op-002, set, confirm
   - SOP says: the decisions are not symmetrical; a rejected order can still be received.
   - Code does: receiving a rejected order is refused until someone uses **Reopen**, which requires a reason.
   - New text: "An intake decision is final: a received order can no longer be rejected, and a rejected one cannot be received until someone presses **Reopen** and gives a reason, which puts it back in **Unaccepted orders**."
   - Evidence: `packages/api/src/modules/collective-order/intake-guard.ts:129` "This order was already rejected... Reopen it with a reason before receiving it."; `packages/api/src/routers/collective-order.ts:3570` `reason: z.string().trim().min(1).max(500)`; `apps/web/src/components/reopen-dialog.tsx:97` `<TooltipContent>Reopen</TooltipContent>`

3. **Step 13, golden 1 detail** - op-003, set, confirm
   - SOP says: the pane has no link to open the order.
   - Code does: the pane shows **Open full page** when the order's trade has a record page.
   - Evidence: `apps/web/src/routes/_next/order.to-receive.tsx:1862` `{readingPattern ? (`; `:1865` "Open full page"

4. **Step 13, golden 5 (whole step)** - op-004, set, confirm
   - SOP says: "Note the order number, then open the matching trade ledger". The overflow offers only **Open details**.
   - Code does: the overflow offers **Open details** and **Open full page**. Open full page is left out when the trade has no record page. The src anchor moves to `order.to-receive.tsx:496`.
   - Evidence: `apps/web/src/routes/_next/order.to-receive.tsx:487` "Open details"; `:492` `{orderRecordPattern(order.businessTypeRaw) ? (`; `:496` "Open full page"

5. **Step 14, golden 5 action** - op-007, set, confirm
   - SOP says: the label renders as "Containers (case type &amp;amp; TEU)" (the label really does render that way).
   - Code does: the label is now a plain "Containers (case type & TEU)".
   - Evidence: `apps/web/src/components/order-form.tsx:2859` `{lineEditor("Containers (case type & TEU)", ...`

6. **Step 14, golden 5 src** - op-008, reanchor, auto
   - SOP says: `order-form.tsx:2590`. Code does: now at :2859.
   - Evidence: `apps/web/src/components/order-form.tsx:2859` `{lineEditor("Containers (case type & TEU)", ...`

7. **Step 14, golden 6 src** - op-009, reanchor, auto
   - SOP says: `order-form.tsx:2699`. Code does: the Save button is now at :2969.
   - Evidence: `apps/web/src/components/order-form.tsx:2969` `onClick={onSave}`; `:2013` `const saved = isEdit ? "Order updated" : "Order created";`

8. **Step 14, golden 6 detail** - op-010, set, confirm
   - SOP says: Save sends changed fields and returns you to the ledger. Nothing about review.
   - Code does: Save is disabled while the order is under review, and its tooltip says why.
   - New sentence added: "While the order is under review **Save** is disabled and reads **Under review. Withdraw it from the trade ledger's review menu to make changes.**"
   - Evidence: `apps/web/src/components/order-form.tsx:2970` `disabled={saving || (isEdit && !existing.data) || underReview}`; `:2972` `title={underReview ? UNDER_REVIEW_SENTENCE : undefined}`; `apps/web/src/components/order-ledger/under-review-notice.tsx:24` "Under review. Withdraw it from the trade ledger's review menu to make changes."

9. **Step 15, golden 0 src** - op-013, reanchor, auto
   - SOP says: `order-row-actions.tsx:135`. Code does: now at :188.
   - Evidence: `apps/web/src/components/order-ledger/order-row-actions.tsx:188` `{isReview ? null : <ReviewMenu iconOnly ... />}`

10. **Step 15, golden 4 src** - op-014, reanchor, auto
    - SOP says: `reject-reason-dialog.tsx:78`. Code does: the title literal now lives in REJECT_COPY at :51.
    - Evidence: `apps/web/src/components/reject-reason-dialog.tsx:51` `title: "Reason for rejection",`; `:104` `<DialogTitle>{copy.title}</DialogTitle>`

11. **Step 17, golden 3 src** - op-017, reanchor, auto
    - SOP says: `lading/$id/index.tsx:399`. Code does: the Check Out handler is at :400.
    - Evidence: `apps/web/src/routes/_next/lading/$id/index.tsx:400` `onAct={() => checkOut.mutate({ ladingId: id })}`; `packages/api/src/routers/lading.ts:582` "On hold — release the hold before checking out"

12. **Step 21, golden 7 src** - op-019, reanchor, auto
    - SOP says: `reject-reason-dialog.tsx:54`. Code does: the dialog copy now lives in REJECT_COPY at :51.
    - Evidence: `apps/web/src/components/reject-reason-dialog.tsx:51` `title: "Reason for rejection",`; `:53` `label: "Reason",`; `:55` `confirm: "Reject",`

13. **Step 22, golden 3 src** - op-020, reanchor, auto
    - SOP says: `cost-lines.tsx:1191`. Code does: the preview label is now at :1192.
    - Evidence: `apps/web/src/routes/_next/expenses/cost-lines.tsx:1192` ": `${groups.length} bills will be created`}"

## Screenshots stale

Ops (all need a re-capture; none change text):

1. **Step 13, /guide/12/shots/0** - op-006, shot, confirm. Code does: new **Open full page** and **Reopen** controls on To Receive. Evidence: `apps/web/src/routes/_next/order.to-receive.tsx:1865` "Open full page"; `:464` `<ReopenDialog`
2. **Step 14, /guide/13/shots/0** - op-011, shot, confirm. Code does: the order form layout changed and there is a new under-review notice. Evidence: `apps/web/src/components/order-form.tsx:531` `status: "Order status",`; `:2471` `{underReview ? <UnderReviewNotice /> : null}`
3. **Step 12, /guide/11/shots/0** - op-012, shot, confirm. Code does: the shared order-form shot is stale. Evidence: `apps/web/src/components/order-form.tsx:531` `status: "Order status",`
4. **Step 16, /guide/15/shots/0** - op-016, shot, confirm. Code does: the B/L create form gained an OCR "Apply anyway" confirmation. Evidence: `apps/web/src/routes/_next/lading/create.tsx:1711` "Apply anyway: I have checked the flagged values against the scan"
5. **Step 19, /guide/18/shots/0** - op-018, shot, confirm. Code does: a new source notice banner on Customs Tracking. Evidence: `apps/web/src/routes/_next/customs-tracking.tsx:191` `data-testid="source-notice"`
6. **Step 25, /guide/24/shots/0** - op-021, shot, confirm. Code does: the Invoice Record tiles changed. Evidence: `apps/web/src/routes/_next/expenses/invoices.tsx:945` `{ label: "Issued (this page)", ... }`
7. **Step 26, /guide/25/shots/0** - op-022, shot, confirm. Code does: a new Edit row control on Pay and Receive. Evidence: `apps/web/src/routes/_next/expenses/payments.tsx:1111` `title="Edit"`
8. **Step 26, /guide/25/shots/1** - op-023, shot, confirm. Code does: new columns on Write-off Management. Evidence: `apps/web/src/routes/_next/expenses/write-offs.tsx:369` `header: "Void reason",`
9. **Step 27, /guide/26/shots/0** - op-026, shot, needs-human. Code does: Financial Stat probably changed (the header now wraps, 2026-09-24), but the cited Profit statement card predates the 2026-09-13 shot, so this quote does not prove it. Evidence: `apps/web/src/routes/_next/report/financial/index.tsx:1849` `<CardTitle ...>Profit statement</CardTitle>`

Script flags (file changed after the shot was taken; the ops above cover some of them):

| Step | Shot file | Changed source |
|---|---|---|
| 12 | customer-intake-sop/shots/step-14.jpg | apps/web/src/components/order-form.tsx |
| 13 | customer-intake-sop/shots/step-13.jpg | apps/web/src/routes/_next/order.to-receive.tsx |
| 14 | customer-intake-sop/shots/step-14.jpg | apps/web/src/components/order-form.tsx |
| 16 | customer-intake-sop/shots/step-16.jpg | apps/web/src/routes/_next/lading/create.tsx |
| 17 | customer-intake-sop/shots/step-17.jpg | apps/web/src/routes/_next/lading/index.tsx |
| 19 | customer-intake-sop/shots/step-19.jpg | apps/web/src/routes/_next/customs-tracking.tsx |
| 21 | customer-intake-sop/shots/step-21.jpg | apps/web/src/routes/_next/expenses/cost-lines.tsx |
| 22 | customer-intake-sop/shots/step-22.jpg | apps/web/src/routes/_next/expenses/cost-lines.tsx |
| 23 | customer-intake-sop/shots/step-23.jpg | apps/web/src/routes/_next/expenses/bills.tsx |
| 24 | customer-intake-sop/shots/step-24.jpg | apps/web/src/routes/_next/expenses/bills.tsx |
| 25 | customer-intake-sop/shots/step-25.jpg | apps/web/src/routes/_next/expenses/invoices.tsx |
| 26 | customer-intake-sop/shots/step-26.jpg | apps/web/src/routes/_next/expenses/payments.tsx |
| 26 | customer-intake-sop/shots/step-26-writeoffs.jpg | apps/web/src/routes/_next/expenses/write-offs.tsx |
| 27 | customer-intake-sop/shots/step-27.jpg | apps/web/src/routes/_next/report/financial/index.tsx |
| 27 | customer-intake-sop/shots/step-27-business.jpg | apps/web/src/routes/_next/report/financial/index.tsx |

Flagged with no op, so re-capture is at the owner's discretion: steps 17, 21, 22, 23, 24 and step-27-business.

## Needs human

Stale guide fixes. The code now appears to fix each of these defects, so there is no page-text op. **Run /zyt-audit** to re-verify the ledger and update fix status.

1. **Step 13, /guide/12/fixes/0/src** - intake decisions asymmetrical (cited `collective-order.ts:3335`). Code does: receive refuses rejected orders, and a reopen verb exists. sop.json already marks this fixed. Evidence: `packages/api/src/modules/collective-order/intake-guard.ts:129` "This order was already rejected... Reopen it with a reason before receiving it."; `packages/api/src/routers/collective-order.ts:3568` `reopen: orgProcedure`; absent from collective-order.ts: `orderAuditedBy: context.org.membership.id,`
2. **Step 15, /guide/14/fixes/2/src** - the legacy `collectiveOrder.review` verb (cited `collective-order.ts:3380`). Code does: there is no longer a review procedure on the router. Evidence: `packages/api/src/routers/collective-order.ts:3485` `reject: orgProcedure`; absent: `review: orgProcedure`
3. **Step 16, /guide/15/fixes/0/src** - a B/L could be set to Released/On-hold from a dropdown. Code does: the status options are now draft/confirmed/cancelled only. Evidence: `apps/web/src/routes/_next/lading/$id/edit.tsx:176` `const STATUS_OPTIONS = SETTABLE_STATUS_OPTIONS;`
4. **Step 16, /guide/15/fixes/1/src** - a B/L under review stayed editable. Code does: `lading.update` now calls assertNotUnderReview. Evidence: `packages/api/src/routers/lading.ts:1693` `await assertNotUnderReview(tx, organizationId, "lading", ladingId);`
5. **Step 16, /guide/15/fixes/2/src** - OCR issues were thrown away. Code does: issues are kept, and an error needs "Apply anyway". Evidence: `apps/web/src/routes/_next/lading/create.tsx:353` `issues: OcrIssue[];`; `:1711` "Apply anyway: I have checked the flagged values against the scan"
6. **Step 20, /guide/19/fixes/0/src** - unnamed fee rows were dropped silently. Code does: the save-time `named` filter is gone. The remaining costName filter at :808 is only in the copy-across path. sop.json marks this partly-fixed. Evidence: `apps/web/src/routes/_next/order.$orderId.expenses.tsx:808` `.filter((r) => r.costName.trim() !== "")`; absent: `const named = all.filter(...)`
7. **Step 21, /guide/20/fixes/0/src** - the fee review gate was skipped when billing without ticked rows. Code does: createBill passes the resolved costLineIds to assertGatesCleared. Evidence: `packages/api/src/routers/expense/cost-lines.ts:2446` `await assertGatesCleared(tx, organizationId, "cost_line", costLineIds, "create_bill");`
8. **Step 21, /guide/20/fixes/1/src** - an approved fee's exchange rate could be rewritten in bulk. Code does: exchangeRateBatch skips frozen rows and reports them. Evidence: `packages/api/src/routers/expense/cost-lines.ts:1983` "// Step 21 D5-A: a fee under review, or approved under a flow that locks"

Break drift with no op (a human rewrite is needed):

9. **Step 20, /breaks/5 "The customer's identity ends here"** - SOP says: the settlement party is written as a bare string, and the company reference is not accepted on this path. Code does: saveChildren sets settlementCompanyId from the order's client company on receivable rows. The downstream effect on the invoice buyer was not re-verified. Evidence: `packages/api/src/routers/collective-order.ts:4908` `? { settlementCompanyId: order.clientCompanyId }`

Needs-human items that have ops (details above):

10. **Step 22, /breaks/6** - op-024 (rewrite the body; the Cost Detail link is now filtered, but there is still no Create bill on the order). Evidence: `apps/web/src/routes/_next/order.$orderId.expenses.tsx:1077`
11. **/chain/ariaLabel** - op-025 (the severed-hand-off count depends on which break ops are accepted). Evidence: `apps/web/src/components/order-ledger/order-record-page.tsx:152`; `apps/web/src/routes/_next/order.$orderId.expenses.tsx:1077`
12. **Step 27, shot step-27.jpg** - op-026 (the evidence predates the shot). Evidence: `apps/web/src/routes/_next/report/financial/index.tsx:1849`

## Refuted (dropped, for the record)

None.

## Run notes

- Findings that held: 16. sop.json has no rules[], so no rule-violation ops were produced.
- Stale guide fixes reported as "run /zyt-audit" with no op: the s13 asymmetry, the s15 legacy review verb, the three s16 fixes, the s20 unnamed rows, and both s21 fixes.
- Break ops change indexes: remove /breaks/3 before /breaks/2. /breaks/6 is rewritten (op-024), not removed. So the ariaLabel count is "Six" (op-025), not the "Five" the checker notes assumed. Removing /breaks/3 drops the page's only `lead: true` break, so pick a new lead. /breaks/5 has drifted but has no op.
- The dry-run validator warns that op-024 is marked chain:true but is a text edit (it should be chain:false).
- These src-changed flags are false positives: the cited line still holds the right code and only the stored baseline anchor text differs. They are marked holds and need a baseline refresh, not an edit: 16 golden/3, 18 golden/3, 18 fixes/0, 22 golden/7, 23 golden/1, 25 golden/5, 26 golden/10.
- Shot staleness was judged from the git log and diff since 2026-09-13. The reflog only goes back to 2026-09-23. Steps 1-11 were not checked, so no verifiedAt is recorded for this run.
- Evidence verify: 26 ops kept, 0 lines rewritten, 0 rejected.

## Dry run

`sop-patch.mjs --dry --source all-light` (default `--accept auto`): **8 would apply, 18 skipped, 0 refused**. No rule conflicts. Validation ok: 0 errors, 1 warning (op-024 is chain:true but is a non-structural text edit).

- Would apply (all auto reanchors): op-001 (:764 to :875), op-008 (:2590 to :2859), op-009 (:2699 to :2969), op-013 (:135 to :188), op-014 (:78 to :51), op-017 (:399 to :400), op-019 (:54 to :51), op-020 (:1191 to :1192).
- Skipped, not accepted under `--accept auto`: 15 confirm ops (op-002 to -007, -010 to -012, -015, -016, -018, -021 to -023).
- Skipped as needs-human: op-024, op-025, and op-026 (capture needed).
- Nothing was written.
