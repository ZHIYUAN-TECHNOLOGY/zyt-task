# SOP audit - ZYT-Task/customer-intake-sop - 2026-09-25

tier light · source audit · auto 8 / confirm 2 / decision 2 / needs-human 39 · changeset C:\Project\ZYT-Task\customer-intake-sop\.zyt\audit-2026-09-25.json

Audited code root: C:/Project/NCT/wt-release-r1 (HEAD f000ac28). **The registry's code root for this page is C:\Project\NCT\nct-layout (HEAD 9e5662be).** See Dry run: pass `-CodeRoot C:/Project/NCT/wt-release-r1` when you apply.

## Rule violations

None.

## New defects

- **op-001 · confirm** · unenforced-approval · steps 21 and 23 · sev money
  **The accountant who submits a bill can sign its first review stage themselves.** Fees (cost_line) have separationOfDuties on. Bills have it off, so decide() never compares the reviewer with the submitter. The seeded Bill flow puts `accounting` on stage 1, and the accounting role holds the whole `expense` subtree, which covers both submit and review. In an org with one accountant, stage 1 passes on one pair of eyes. The project's own concurrency test says an owner may review their own bill.
  Evidence:
  - packages/api/src/modules/audit/resources.ts:690 `separationOfDuties: false,` (bill)
  - packages/api/src/modules/audit/resources.ts:638 `separationOfDuties: true,` (cost_line, for contrast)
  - packages/api/src/modules/audit/decide.ts:59 `if (entry.separationOfDuties && submission.submittedBy === org.membership.id) {`
  - packages/api/src/modules/audit/seed.ts:225 `{ roles: ["accounting"], quorum: "all_pass_any_reject" },`
  - packages/api/src/roles.ts:175 `"expense",`
  - packages/api/src/routers/expense.bill-review.concurrency.test.ts:211 `// Bill has separationOfDuties off, so the owner may also be its reviewer.`
  Refuter: holds.

- **op-002 · needs-human** · fake-default · step 20 (also step 22) · sev money
  **Copy to payable fills every vendor cost with our selling price.** copyAcross spreads the whole receivable row (`...r`) into each payable row. It blanks only the settlement unit and the exchange rate, so quantity, unit price and amount come across as the customer's figures. SOP step 20 then tells the operator to type only the vendor.
  Evidence:
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:801 `const copyAcross = (`
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:813 `...r,`
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:830 `settlementUnit: toPayable ? "" : r.settlementUnit,`
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:986 `onCopyAcross={() => copyAcross(receivable, setPayable, true)}`
  Refuter: PLAUSIBLE. The code matches the claim. But this is an explicit mirror action that may be meant to copy the price (likely copied from eyun). "Nothing flags it" is overstated, because the SINGLE BILL TOTAL panel would show gross profit near 0. The defect is real, but it is not strictly a fake default.

- **op-003 · needs-human** · fake-default · steps 20 and 6 · sev money
  **Every new fee row starts in USD, whatever currency the branch works in.** New expense rows and new quotation lines both hard-code USD. The branch currency (team.currency) exists but is not used as the default.
  Evidence:
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:366 `currency: "USD",`
  - apps/web/src/routes/_next/order.$orderId.expenses.tsx:360 `// OURS, NOT OBSERVED: eyun populates `Currency` from the`
  - apps/web/src/routes/_next/quotations/$quotationId.tsx:393 `currency: "USD",`
  Refuter: PLAUSIBLE. The default is deliberate and commented, and the SOP already lists it as a step 20 pitfall ("Default currency is USD on every new row"). This is a documented hazard rather than a hidden one.

- **op-004 · confirm** · fake-default · steps 26 and 10 · sev data
  **Before 8 am the payment date and decision date default to yesterday.** Both dates are pre-filled with `new Date().toISOString().slice(0, 10)`, which is the UTC date. In Malaysia (UTC+8), anything entered from 00:00 to 07:59 gets the previous day. A receipt keyed early on the 1st of the month therefore lands in the previous month.
  Evidence:
  - apps/web/src/routes/_next/expenses/payments.tsx:205 `paymentDate: new Date().toISOString().slice(0, 10),`
  - apps/web/src/components/quotation/decide-quotation-dialog.tsx:62 `setDecidedOn(new Date().toISOString().slice(0, 10));`
  Refuter: holds.

## Ledger status changes (decision: accept by explicit op id only)

Only op-005 and op-007 are true `decision` ops. Every other transition below was lowered to `needs-human`: a refuter called it PLAUSIBLE, or the refuter cap (1 agent x 8) was reached before anyone checked it independently. sop-patch never applies a needs-human op.

- **op-005 · decision** · ledger `fx-gain-loss` (/ledger/items/1) · step 26 · open -> **fixed**
  The payment form now sends an exchange rate. The write-off treats a stored rate of 1 as unknown and looks the rate up in the org table instead. settlementFx books no gain or loss when a rate is unknown, and reports which rate was missing (fxUnknown).
  Evidence: apps/web/src/routes/_next/expenses/payments.tsx:336 `exchangeRate: rate || undefined,` · packages/api/src/routers/expense/write-offs.ts:539 `toNumber(pay.exchangeRate, 1) === 1` · packages/api/src/routers/expense/write-offs.ts:867 `const fx = settlementFx({`
  Refuter: holds. Fingerprint 0fd12698d6a3... (manifest sha of payments.tsx).
- **op-007 · decision** · ledger `lump-sums` (/ledger/items/3) · step 12 (quote to cost lines) · open -> **fixed**
  The bridge now carries quantity and unit price whenever quantity x price equals the amount. Lines raised to a minimum charge stay as lump sums, on purpose.
  Evidence: packages/api/src/modules/expense/bridge.ts:236 `quantity: breakdownIsHonest ? feeLine.quantity : null,`
  Refuter: holds. The manifest has no entry for bridge.ts, so the op carries commit f000ac28 (wt-release-r1 HEAD) instead of a fingerprint.
- **op-006 · needs-human** · `never-overdue` (/ledger/items/2) · open -> fixed. Bills get a settlement company from the line, or from an exact match on the settlement-unit name, so the 0062 trigger can set a due date. Evidence: packages/api/src/routers/expense/cost-lines.ts:2514 `match ? { id: match.id, resolvedBy: "name" } : { id: null, resolvedBy: null },` · cost-lines.ts:2606 `settlementCompanyId: companies[index]!.id,`. Refuter: PLAUSIBLE. Converted cost lines do not carry the quote's company, and payable units are free text matched case-sensitively, so some happy-path bills still never go overdue. partly-fixed fits better than fixed.
- **op-009 · needs-human** · `bill-rewrite` (/ledger/items/5) · open -> partly-fixed. The retotal writer is blocked while a bill is under review and frozen after approval. The seeded flow still ticks only two gates, so cancel_write_off is not seeded. Evidence: packages/api/src/routers/expense/bills.ts:587 `gates: [underReviewGate("bill"), billTotalPostApprovalGate],` · packages/api/src/modules/audit/seed.ts:234 `gates: ["input_invoice_no", "write_off"],`. Refuter: cap reached, not checked.
- **op-010 · needs-human** · `base-currency` (/ledger/items/6) · open -> partly-fixed. There is now an optional org base currency and a warning for mixed currencies. When the base currency is unset, reports fall back to the branch currency and the figures are unchanged. Evidence: packages/db/src/schema/setting.ts:88 `baseCurrency: text("base_currency"),` · packages/api/src/modules/setting/fx-settings.ts:223 `return reportingCurrencyFrom(base, row?.currency);` · packages/api/src/routers/report.ts:1373 `if (distinct.length > 1) mixedLocalCurrencies = distinct;`. Refuter: cap reached, not checked.
- **op-011 · needs-human** · `unnumbered` (/ledger/items/7) · open -> fixed. convertToOrder allocates a job number. Evidence: packages/api/src/routers/quotation.ts:5152 `const order = await insertOrderWithJobNumber(tx, context.org, {` · quotation.ts:5201 `const orderNo = order.jobNumber ?? q.quotationNo;`. Refuter: cap reached, not checked.
- **op-012 · needs-human** · `never-closed` (/ledger/items/8) · open -> partly-fixed. The row menu now has End job, Cancel end, Lock and Unlock. Shut out, archive and delete still have no web caller. Evidence: apps/web/src/components/order-ledger/order-row-actions.tsx:306 `await client.collectiveOrder.batch({ action: move.action, orderIds: [orderId] });` · order-row-actions.tsx:113 `* comes back, and shut out, archive and delete stay off this menu.`. Refuter: cap reached, not checked.
- **op-013 · needs-human** · `paperwork` (/ledger/items/9) · open -> partly-fixed. Document linking now works, and the documents card is mounted on the order. There is still no K1/K2 document type. Evidence: apps/web/src/routes/_next/documents.$documentId.tsx:1192 `client.document.link({ documentId, entityType: target.kind, entityId: target.id }),` · apps/web/src/components/order-ledger/order-ledger-page.tsx:1213 `lead: <OrderDocumentsCard rollup={readingDocuments} showHeader={false} />,` · packages/document-ai/src/registry/types.ts:5 `export type DocTypeId = "mbl_hbl" | "commercial_invoice" | "packing_list" | "unknown";`. Refuter: cap reached, not checked.
- **op-014 · needs-human** · `island` (/ledger/items/10) · open -> fixed. The customs payload carries countdownDeadline, and the card shows the 72h clock. Evidence: packages/api/src/routers/collective-order.ts:1903 `countdownDeadline: Date | null;` · collective-order.ts:1992 `countdownDeadline: row.countdownDeadline,` · order-ledger-page.tsx:1213 (as above). Refuter: cap reached, not checked.
- **op-015 · needs-human** · `dead-queues` (/ledger/items/11) · open -> partly-fixed. To Receive now links to the order, and the B/L page can submit for review. The company qualification and contract flows are still `enabled: false`. Evidence: apps/web/src/routes/_next/order.to-receive.tsx:1863 `<Link to={readingPattern} params={{ orderId: reading.id }}>` · apps/web/src/routes/_next/lading/$id/index.tsx:453 `{canUpdate && data && <ReviewMenu selectedIds={[id]} onReview={reviewLading} />}` · packages/api/src/modules/audit/seed.ts:103 and :150 `enabled: false,`. Refuter: cap reached, not checked.
- **op-016 · needs-human** · `channels` (/ledger/items/12) · open -> partly-fixed. Quotations record enquiryChannel. There is still no enquiry record for non-email channels and no won/lost thread status. Evidence: packages/db/src/schema/quotation.ts:257 `"whatsapp",` · packages/db/src/schema/mailbox.ts:61 `"closed",`. Refuter: cap reached, not checked.
- **op-018 · needs-human** · `job-shape` (/ledger/items/14) · open -> partly-fixed. Order status is now validated against the trade. create still requires only branch and business type. Evidence: packages/api/src/routers/collective-order.ts:2536 `assertOrderStatus(rest.businessType, rest.status, null);` · collective-order.ts:2515 `owningBranchId: z.string().min(1),`. Refuter: cap reached, not checked.
- **op-019 · needs-human** · `credit` (/ledger/items/15) · open -> fixed. Invoicing refuses when a company is over its limit, unless an override reason is given. Evidence: packages/api/src/routers/expense/bills.ts:2131 `if (overages.length > 0 && !input.creditOverrideReason) {`. Refuter: cap reached, not checked.
- **op-024 · needs-human** · guide step 4 fix `s04-the-tariff-gate-is-declared-but-never-enforced` · open -> fixed. Evidence: packages/api/src/modules/audit/seed.ts:170 `gates: ["reference_fee_template"],` · packages/api/src/routers/quotation.ts:4751 `await assertTemplatesReferenceable(context.db, context.org.organizationId, [template.id]);`. Refuter: cap reached, not checked.
- **op-025 · needs-human** · step 5 `s05-quotation-date-is-the-row-s-save-timestamp-and-n` · open -> fixed. Evidence: apps/web/src/routes/_next/quotations/$quotationId.tsx:2199 `<form.Field name="quotationDate">`. Refuter: cap reached, not checked.
- **op-026 · needs-human** · step 5 `s05-quotation-staff-is-auto-stamped-copied-by-duplic` · open -> fixed. Evidence: $quotationId.tsx:2213 `<form.Field name="quotationStaff">` · packages/api/src/routers/quotation.ts:4621 `quotationStaff: defaultQuotationStaff(context),`. Refuter: cap reached, not checked.
- **op-027 · needs-human** · step 6 `s06-applied-tariff-lines-ignore-the-container-mix` · open -> fixed. Evidence: apps/web/src/components/reference-template-dialog.tsx:217 `quantity: seededQuantity(chargeUnit, containers),`. Refuter: cap reached, not checked.
- **op-028 · needs-human** · step 7 `s07-an-ambiguous-tariff-silently-drops-the-floor` · open -> partly-fixed. The strictest cell now wins. A unit spelt differently still resolves to none, with no floor. Evidence: packages/api/src/quotation/tariff-check.ts:37 `* Now the STRICTEST cell wins: among the tied latest cells, the highest` · tariff-check.ts:489 `tariffUnits: units ? [...units].sort().slice(0, 3) : null,` · tariff-check.ts:626 `const offending = result.lines.filter((line) => line.status === "below_tariff");`. Refuter: cap reached, not checked.
- **op-029 · needs-human** · step 8 `s08-a-quotation-stays-fully-editable-while-it-is-sit` · open -> fixed. Evidence: quotation.ts:4288 `await assertNotUnderReview(tx, context.org.organizationId, "quotation", id);` · quotation.ts:4711 `await assertNotUnderReview(context.db, organizationId, "quotation", input.quotationId);`. Refuter: cap reached, not checked.
- **op-030 · needs-human** · step 8 `s08-the-seeded-quotation-flow-lets-the-branch-manage` · open -> fixed. Evidence: decide.ts:59 (as op-001) · resources.ts:245 `separationOfDuties: true,`. Refuter: cap reached, not checked.
- **op-031 · needs-human** · step 8 `s08-the-legacy-quotations-review-verb-stamps-approve` · open -> fixed. Evidence: quotation.ts:5454 `listPendingDecisionCorrections: orgProcedure`, with `review: orgProcedure` absent. Refuter: cap reached, not checked.
- **op-032 · needs-human** · step 8 `s08-submit-then-retract-permanently-unlocks-an-appro` · open -> fixed. Evidence: packages/api/src/modules/audit/submit.ts:59 `await assertPostApprovalEditable(tx, org.organizationId, input.resourceType, input.resourceId, {`. Refuter: cap reached, not checked.
- **op-033 · needs-human** · step 8 `s08-the-export-quotation-gate-is-wired-to-reading-a-` · open -> fixed. Evidence: quotation.ts:2742 and :2909 `"export",`. Refuter: cap reached, not checked.
- **op-034 · needs-human** · step 9 `s09-there-is-no-outbox-so-a-failed-stamp-costs-a-sec` · open -> fixed. Evidence: quotation.ts:2878 `sendKey: z.string().uuid(),`. Refuter: cap reached, not checked.
- **op-035 · needs-human** · step 10 `s10-a-mis-keyed-decision-cannot-be-corrected` · open -> fixed. Evidence: quotation.ts:3790 `requestDecisionCorrection: orgProcedure`. Refuter: cap reached, not checked.
- **op-036 · needs-human** · step 13 `s13-intake-decisions-are-asymmetrical` · open -> fixed. Evidence: packages/api/src/modules/collective-order/intake-guard.ts:46 `receive: "unaccepted",` · collective-order.ts:3568 `reopen: orgProcedure`. Refuter: cap reached, not checked.
- **op-037 · needs-human** · step 15 `s15-a-collective-order-stays-fully-editable-while-it` · open -> fixed. Evidence: collective-order.ts:3009 `await assertNotUnderReview(tx, organizationId, "collective_order", orderId);` · collective-order.ts:2823 and :4092 `await assertNotUnderReview(tx, organizationId, "collective_order", input.orderId);`. Refuter: cap reached, not checked.
- **op-038 · needs-human** · step 15 `s15-order-approval-freezes-nothing-downstream-so-app` · open -> partly-fixed. The gates are seeded, but only for new orgs, and expense_entry stays unticked. Evidence: seed.ts:191 `gates: ["lock", "end_order", "shut_out", "create_lading"],` · seed.ts:192 `gatesOnlyForNewOrg: true,` · packages/api/src/routers/lading.ts:1121 `"create_lading",`. Refuter: cap reached, not checked.
- **op-039 · needs-human** · step 15 `s15-the-legacy-collectiveorder-review-verb-lets-ops-` · open -> fixed. Evidence: collective-order.ts:3485 `reject: orgProcedure`, with `review: orgProcedure` absent. Refuter: cap reached, not checked.
- **op-040 · needs-human** · step 16 `s16-a-bill-of-lading-can-be-set-to-released-or-on-ho` · open -> fixed. Evidence: apps/web/src/routes/_next/lading/-status-echo.ts:17 `export const SETTABLE_STATUS_OPTIONS = ["draft", "confirmed", "cancelled"];` · lading.ts:1678 `assertSettableStatus(statusChange);`. Refuter: cap reached, not checked.
- **op-041 · needs-human** · step 16 `s16-a-bill-of-lading-under-review-stays-fully-editab` · open -> fixed. Evidence: lading.ts:1693 `await assertNotUnderReview(tx, organizationId, "lading", ladingId);`. Refuter: cap reached, not checked.
- **op-042 · needs-human** · step 16 `s16-ocr-validation-issues-are-computed-returned-and-` · open -> fixed. Evidence: apps/web/src/routes/_next/lading/create.tsx:1676 `disabled={!canApplyOcr(ocrIssues, ocrAcknowledged, ocrUpload.isPending)}`. Refuter: cap reached, not checked.
- **op-043 · needs-human** · step 16 `s16-linking-the-approved-order-carries-only-its-job-` · open -> fixed. Evidence: create.tsx:567 `const { next, filled } = prefillFromOrder({ ...form, orderId }, picked, fromOrder);`. Refuter: cap reached, not checked.
- **op-045 · needs-human** · step 20 `s20-unnamed-fee-rows-are-dropped-without-a-word` · open -> fixed. Evidence: order.$orderId.expenses.tsx:740 `const payProblems = feeRowProblems(payable);` · :759 `const named = all.filter((r) => !isUntouchedNewRow(r));`. Refuter: cap reached, not checked.
- **op-046 · needs-human** · step 21 `s21-the-fees-must-pass-review-before-billing-gate-is` · open -> fixed. Evidence: cost-lines.ts:2446 `await assertGatesCleared(tx, organizationId, "cost_line", costLineIds, "create_bill");`. Refuter: cap reached, not checked.
- **op-047 · needs-human** · step 21 `s21-an-approved-fee-s-exchange-rate-can-still-be-rew` · open -> fixed. Evidence: cost-lines.ts:1991 `const targets = rows.filter((r) => !isClaimed(r) && !freezes.has(r.id));`. Refuter: cap reached, not checked.
- **op-048 · needs-human** · step 21 `s21-an-approved-fee-can-be-deleted-outright-approval` · open -> fixed. Evidence: cost-lines.ts:419 `underReviewGate("cost_line"),` · cost-lines.ts:1653 `const freezes = await costLineReviewFreezes(tx, organizationId, ids);`. Refuter: cap reached, not checked.
- **op-049 · needs-human** · step 21 `s21-a-fee-sitting-in-the-review-queue-is-fully-edita` · open -> fixed. Evidence: cost-lines.ts:349 `costLineContentUnderReviewGate,`. Refuter: cap reached, not checked.
- **op-050 · needs-human** · step 22 `s22-bills-cannot-be-split-per-order-though-the-serve` · open -> fixed. Evidence: apps/web/src/routes/_next/expenses/cost-lines.tsx:1236 `checked={groupByOrder}` · cost-lines.tsx:1321 `groupByOrder,`. Refuter: cap reached, not checked.

## Ledger re-verified (auto: lastVerified only)

Each of these sets `lastVerified: 2026-09-25` and leaves the status open. No refuter is needed for this tier.

- **op-008 · auto** · `two-profits` (/ledger/items/4) · step 27. Financial Stat converts with bill.exchangeRate, while the order footer sums each row's own local amount. Evidence: packages/api/src/routers/report.ts:2205 ``amount: sql<string>`coalesce(sum(${bill.totalAmount}::numeric * coalesce(${bill.exchangeRate}::numeric, 1)), 0)`,`` · order.$orderId.expenses.tsx:848 `rows.reduce((s, r) => s + feeLocalAmount(r, taxInclusive), 0);`
- **op-017 · auto** · `money-by-hand` (/ledger/items/13) · steps 24-26. The invoices router has no send verb, no permalink and no paid status. Evidence: packages/api/src/routers/expense/invoices.ts:616 `markDownloaded: orgProcedure`, with `send: orgProcedure` absent.
- **op-020 · auto** · `tax-invoice` (/ledger/items/16) · step 24. The seller's tax ID is hard-coded to null, and there is no SST handling. Evidence: packages/api/src/modules/export/invoice-document.ts:385 `taxId: null,`
- **op-021 · auto** · `haulage` (/ledger/items/17). There is no driver, dispatch or proof-of-delivery entity, and no link from a truck to an order. Evidence: packages/db/src/schema/fleet.ts:25 `export const fleetDevices = pgTable(`
- **op-022 · auto** · step 2 `s02-duplicate-companies-enter-through-case`. Evidence: packages/db/src/schema/company.ts:138 `uniqueIndex("company_org_name_uidx").on(table.organizationId, table.name),`
- **op-023 · auto** · step 3 `s03-a-contact-nobody-ticked-as-primary-never-reaches`. Evidence: report.ts:1107 `eq(companyContact.isPrimary, true),` · packages/api/src/routers/company-contact.ts:181 `if (input.isPrimary) {`
- **op-044 · auto** · step 18 `s18-there-is-no-customs-declaration-type`. Evidence: packages/document-ai/src/registry/types.ts:5 `export type DocTypeId = "mbl_hbl" | "commercial_invoice" | "packing_list" | "unknown";`
- **op-051 · auto** · step 27 `s27-the-month-end-documents-have-no-menu-entry`. Evidence: apps/web/src/components/shell-next/nav.ts:257 `{ to: "/report/financial", label: "Financial Stat", p: "report:read" },` · apps/web/src/components/export-table-dialog.tsx:97 `{ value: "debit_note", label: "DEBIT_NOTE", hint: "Debit note — 可与国外客户对账" },` · cost-lines.tsx:2274 `<ExportTableDialog`

## Needs human

None beyond the ops above. There are 39 needs-human ops in total, each listed under its own section with tier needs-human: op-002, op-003, op-006, op-009 to op-016, op-018, op-019, op-024 to op-043, and op-045 to op-050. No note stands without an op.

## Refuted (dropped)

- **report-only** · ledger `identity` ("The customer's identity does not survive the journey") · proposed open -> fixed. **Dropped.** The quote-conversion path still loses the company: mapFeeLineToCostLine carries the settlementUnit text but not settlementCompanyId. saveChildren stamps the company only on Expense-entry receivable rows, and createBill recovers it only when the names match exactly (case-sensitive). The quoted fixes do exist, but they do not cover every path. partly-fixed would hold; fixed does not. Consider a partly-fixed transition in a later run.

## Rule enforcement notes (proposals for /zyt-update --rules, never applied by the audit)

None.

## Coverage

What was checked:
- **Scope.** One hunter, looking for unenforced-approval, role-mismatch, handoff-drop, unreachable-screen and fake-default. It read the sop.json guide (roles, routes, golden steps 1-27).
- **Gate keys.** Every declared gate key in modules/*/gates.ts was checked against its assertGate(s)Cleared callers:
  - quotation: export, convert, copy
  - fee_template: reference
  - collective_order: lock, unlock, end, shut_out, create_lading, duplicate, expense_entry, container, cargo
  - lading: endorsement, amendment, release, detention
  - bill: input_invoice_no, write_off, cancel_write_off, courier, voucher
  - cost_line: reconcile, lock, create_bill, export, write_off
- **Where gates run.** bills.invoice and write-off verify are gated after their row locks. create_lading is enforced on both lading create and update, and there is no other lading insert path.
- **Separation of duties.** separationOfDuties was checked on every entry in REVIEWABLE_RESOURCES, against the seeded flows and the role grants. That produced op-001. Lading is also SoD-off, but its only reviewer role (director) cannot submit.
- **Navigation.** nav.ts gating was checked for the approval queues and the step routes.
- **Buttons checked.** bl-job Void is UI-gated, and by design no static role holds the grant. Assign-number is gated on the server only, by design.
- **Document approval hooks.** These were checked too. Auto-approval happens only under an org's auto_approve policy, and that path still runs the hooks.
- **Defaults.** Expense-entry new-row and copy defaults, quotation line defaults, and payment/decide date defaults were all checked.

Not covered in depth:
- inbox and thread triage (step 1)
- company and contact creation internals (steps 2-3)
- month-end report math (step 27)
- customs-tracking scheduler internals (step 19)

## Runtime proof offered

Offered, not run. The audit never starts the app, a browser or a test suite. To prove the findings in the running app, paste any of these:

```
/e2e-deep --deep /approve/bill
/e2e-deep --deep /expenses/bills
/e2e-deep --deep /order/$orderId/expenses
/e2e-deep --deep /quotations/$quotationId
/e2e-deep --deep /expenses/payments
/audit-prove-a
```

What each one covers:
- /approve/bill and /expenses/bills: step 23, op-001 (whether a bill submitter can decide stage 1).
- /order/$orderId/expenses: step 20, op-002 and op-003.
- /quotations/$quotationId: steps 6 and 10, op-003 and op-004.
- /expenses/payments: step 26, op-004.

## Dry run

The first run used the prescribed command, with no `--code-root`. sop-patch took its code root from the registry (C:\Project\NCT\nct-layout).
- Result: **3 would apply** (op-021, op-022, op-044), **43 skipped**, **5 refused**. Validation: ok=false. Nothing was written.
- The skips are 39 needs-human ops, plus op-001 and op-004 (confirm, not accepted under `--accept auto`) and op-005 and op-007 (decision: explicit op id only).
- Refused, each "quote not within ±3" of its cited line, because nct-layout has the code at an older position:
  - op-008: report.ts:2205, found at 2018
  - op-017: invoices.ts:616, found at 538
  - op-020: invoice-document.ts:385, found at 291
  - op-023: report.ts:1107, found at 958
  - op-051: export-table-dialog.tsx:97, found at 83
- Cause: a code-root mismatch, not bad evidence. The audit and evidence-verify read wt-release-r1 (51 kept, 0 rewritten, 0 rejected). The registry points at nct-layout, which is at a different commit.

A diagnostic re-run of the same dry run with `--code-root C:/Project/NCT/wt-release-r1` gave **8 would apply** (all auto ops), 43 skipped, **0 refused**, validation ok=true.

## How to apply

After you pick op ids, run the command below. Never pass "all". Decision ops (op-005, op-007) need their explicit id. Pass the audited code root, or the auto ops will be refused as shown above.

```
powershell -File "C:\Users\user\.claude\skills\zyt-audit\scripts\audit-apply.ps1" -Page ZYT-Task/customer-intake-sop -Accept auto,<op ids> -CodeRoot C:/Project/NCT/wt-release-r1
```

Candidates: op-001, op-004 (confirm); op-005, op-007 (decision). Needs-human ops cannot be applied until they are re-checked and re-tiered. Afterwards, record the audit date with registry.mjs. Also decide whether the registry's codeRoot for this page should move from nct-layout to wt-release-r1.
